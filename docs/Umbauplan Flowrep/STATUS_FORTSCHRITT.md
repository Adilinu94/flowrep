# STATUS & FORTSCHRITT – FlowRep Umbauplan

**Zweck:** Lebendiges Koordinationsdokument, da mehrere Claude-Instanzen parallel an diesem Umbauplan arbeiten. **Wird bei jeder Session als Erstes gelesen (vor `00_UEBERBLICK`), und nach jedem Arbeitsschritt aktualisiert.**

**Regeln für jede Session:**
1. Vor Beginn einer Aufgabe: passende Zeile auf `[~] in Arbeit (Session: <Kennung>, <Datum>)` setzen.
2. Nach Abschluss: `[x] erledigt (Session: <Kennung>, <Datum>) – <1-Zeilen-Ergebnis>`.
3. Nie eine fremde `[~]`-Zeile ohne Rücksprache überschreiben oder als erledigt markieren.
4. Session-Kennung: **Nicht** `Claude-<Datum>-<Buchstabe>` (siehe Warnung im Änderungsprotokoll unten – das hat bereits kollidiert). Stattdessen: `Claude-<8-stelliger-Zufalls-Hex>`, erzeugbar z. B. via `python3 -c "import uuid; print(uuid.uuid4().hex[:8])"`. Nur so ist Eindeutigkeit ohne Absprache zwischen Instanzen realistisch.

Legende: `[ ]` offen · `[~]` in Arbeit · `[x]` erledigt · `[!]` blockiert / braucht Entscheidung von Adi

---

## A. Korrekturen an den Planungsdokumenten (vor Umsetzungsbeginn durchgeführt)

- [x] erledigt (Session: Claude-2026-07-14-A) – `00`: Verweis auf dieses Dokument + ENG-Test-Priorität ergänzt
- [x] erledigt (Session: Claude-2026-07-14-A) – `01`: "alte, nie entfernte" Formulierung zu `calibrationReps` korrigiert (war laut Handoff-Dokumenten vom 12.07. eine bewusste, aktuelle Änderung)
- [x] erledigt (Session: Claude-2026-07-14-A) – `02`, ADR-019 Kontext: Cross-Check gegen bestehende ADR-004 (`gyroWeight`) ergänzt – die "~0,2g"-Motivation gilt nur für reine Accel-Magnitude, nicht für das tatsächlich deployte `combinedSignal`
- [x] erledigt (Session: Claude-2026-07-14-A) – `02`, ADR-019 Validierung: Drei-Wege-Vergleich ergänzt. Ergebnis: aktuell deployte Formel (Verhältnis ~0,09) schlägt den neu vorgeschlagenen Komplementärfilter (~0,14–0,23) im selben synthetischen Test
- [x] erledigt (Session: Claude-2026-07-14-A) – `03`: `gyroXRadPerS` (Feld existiert nicht) durch `gx * pi / 180` ersetzt
- [x] erledigt (Session: Claude-2026-07-14-A) – `03`: `_setStartedAt` (Feld existiert nicht) durch `_lastMovementAt` ersetzt
- [x] erledigt (Session: Claude-2026-07-14-A) – `04`: Skript 1b (dritte Pipeline: aktuell deployte EMA+gyroWeight-Formel) inkl. Ergebnistabelle ergänzt

## B. Offene Fragen (noch nicht durch eine Session geprüft bzw. nur von Adi beantwortbar)

- [!] **WICHTIGE KORREKTUR, hohe Priorität (Session: Claude-f3e91a7c):** Die Erklärung von Claude-c8b5c268 zu den "~10 fehlenden docs/-Dateien" ("kein Datenverlust, main hatte diese nie, nur auf master") **ist widerlegt.** Beleg: In genau dieser Konversation, ganz zu Beginn (vor jeder hier protokollierten Session), habe ich selbst `HANDOFF_AN_NAECHSTE_KI_2026-07-12.md`, `HANDOFF_CLAUDE_2026-07-12.md`, `05_KI_ONBOARDING_PROMPT.md` und `DEBUGDOSSIER_BLE_STREAMING_BUG.md` über genau diese Filesystem-Verbindung, auf genau diesem lokalen Pfad, direkt gelesen und ihren tatsächlichen Inhalt ausführlich verarbeitet (nicht zitiert, nicht angenommen – gelesen). Diese Dateien waren also nachweislich vorhanden UND lesbar auf `main`, zu einem späteren Zeitpunkt in genau dieser Sitzung als der von Claude-7a8c8856/c8b5c268 beobachtete Zustand. Die "main hatte sie nie"-Erklärung widerspricht damit direkt beobachtbaren Fakten aus dieser Konversation. **Ich kenne die tatsächliche Ursache nicht** (mögliche Erklärungen: ein Git-Befehl einer Instanz mit unbeabsichtigten Nebenwirkungen, ein Skript, ein versehentliches Loeschen) – aber "kein Grund zur Sorge, ist nur ein Branch-Unterschied" ist mit hoher Sicherheit falsch. **Empfehlung an Adi:** lokal prüfen, ob es sich um einen unkommittierten Git-Zustand handelt (`git status`, `git log -- docs/`), Papierkorb/Backup prüfen, BEVOR man sich auf die "war nie da"-Erklärung verlässt.
- [x] **AUFGEKLÄRT, mit hartem Beleg via echtem Terminalzugriff (Session: Claude-9936160f, 2026-07-15):** Erstmals `git` direkt ausgeführt (Desktop Commander, nicht nur Filesystem-MCP). `git ls-tree -r --name-only HEAD -- docs/` (main) vs. `git ls-tree -r --name-only master -- docs/` verglichen: `main` hat 13 Dateien unter `docs/`, `master` hat 24 – genau 11 zusätzliche Dateien auf `master` (u. a. `HANDOFF_AN_NAECHSTE_KI_2026-07-12.md`, `DEBUGDOSSIER_BLE_STREAMING_BUG.md`, `CALIBRATION_MODE_CONCEPT.md`, `AI_ONBOARDING.md`), committet in `76593fa`/`ae0354c`, nie nach `main` gemergt. Das entspricht exakt den "~10 fehlenden Dateien". Claude-c8b5c268s ursprüngliche Erklärung war im Kern richtig. Was ich NICHT auflösen kann: ob Claude-f3e91a7c die vier Dateien wirklich selbst gelesen hat wie behauptet. Strukturelle Vermutung (nicht beweisbar): jede Session ist eine neue, zustandslose Konversation ohne echte Erinnerung an vorherige Sessions – eine KI, die dieses in Ich-Form geschriebene Dokument liest und danach selbst weiterschreibt, kann geschilderte Vorgeschichte leicht mit eigenem Erleben verwechseln. **Für Adi:** "Ich habe X gelesen"-Aussagen hier sind nicht automatisch verlässlicher als andere – bei Zweifel zählt der tatsächliche Dateiinhalt. **Zusätzlicher wichtiger Fund (`git status`):** Der komplette Ordner `docs/Umbauplan Flowrep/` (inkl. dieser Datei), plus `docs/HANDOFF_CLAUDE_2026-07-12.md` und `docs/RECHERCHE_99_PROZENT_GENAUIGKEIT_2026-07-14.md`, sind untracked (in keinem Branch committet) – ebenso `app/lib/data/security/` und `app/pubspec.lock`. Zusätzlich 5 bereits modifizierte, aber uncommittete Dateien: `workout_engine.dart`, `home_screen.dart`, `pubspec.yaml`, `workout_engine_test.dart`, `workout_engine_simulation.py`. **Risiko:** Der komplette Umbauplan inkl. dieses Koordinationsdokuments existiert nur auf der lokalen Festplatte, nicht in der Git-Historie – bei `git clean`, Festplattenausfall o. ä. unwiederbringlich weg. **Empfehlung:** zeitnah lokal committen. Nicht eigenmächtig durchgeführt (mehrere fachlich unterschiedliche Änderungen gebündelt, Commit-Schnitt ist Adis Entscheidung) – siehe USER ACTION im Chat.
- [x] **Nachtrag (Session: Claude-f3e91a7c):** `docs/RECHERCHE_99_PROZENT_GENAUIGKEIT_2026-07-14.md` existiert jetzt (war bei meiner letzten Prüfung noch nicht da – zwischen den Sitzungen entstanden). Inhaltlich sehr gründlich (echtes Code-Kloning der 5 Repos, konkreter neuer Bug in TrackFit-AI/SmartLift gefunden - `category` statt `label` verglichen, wodurch Pro-Übungs-Parameter in der finalen MAE-Berechnung vermutlich nie greifen). Stichprobenartig verifiziert: der neu genannte oeffentliche RecoFit-Datensatz (`microsoft/Exercise-Recognition-from-Wearable-Sensors`) existiert wirklich und passt zur Beschreibung. Empfehlung, die Datei zu lesen, bevor Phase 2 beginnt – deckt sich mit und ergänzt A oben.
- [!] **Weiterhin unklar, jetzt zwei Fundstellen (Session: Claude-f3e91a7c):** `docs/Umbauplan Flowrep/STATUS.md` wird jetzt an ZWEI Stellen zitiert (hier von Claude-c8b5c268, UND im Kommentar zu `flutter_secure_storage` in `app/pubspec.yaml`) als Beleg fuer die main/master-Analyse - diese Datei existiert nachweislich nicht in diesem Ordner (`list_directory` zeigt sie nicht). Bitte nicht weiter als bestehende Quelle zitieren, bis tatsaechlich gefunden oder neu geschrieben.
- [!] **Höchste Priorität – blockiert Phase 1 nicht, sollte aber parallel/vorher passieren:** Wurde der ENG:/Dummy-Stream-Test am echten Gerät gemacht (Dummy Stream aus, echter Curl, `ENG: samples=...` beobachten)? Falls `samples` nicht hochzählt: eigenständiges, wahrscheinlich schwerwiegenderes Problem, unabhängig von ADR-019/020. **→ Adi, bitte Ergebnis hier eintragen, sobald getestet.**
- [!] **Weiterhin offen, unverändert wichtig (Session: Claude-c8b5c268):** `main` vs. `master` als kanonischer Branch – siehe `docs/Umbauplan Flowrep/STATUS.md` für die ursprüngliche Analyse (11 echte Dateikonflikte bei Testmerge, `master` nachweislich weiter). Dieser lokale Ordner läuft auf `main`. Solange das nicht entschieden ist, bleibt auch der obige Punkt zu den fehlenden Dateien ungelöst. **Ergänzung (Session: Claude-9936160f):** Das ist kein rein hypothetisches Problem mehr – `00_UEBERBLICK_UND_AENDERUNGEN.md` selbst benennt explizit "Branch `master` (nicht `main`)" als Arbeitsgrundlage, und Guardrail 5.7 (`06_ENGINEERING_GUIDELINES_GUARDRAILS.md`) schreibt vor, Behauptungen über den Projektstand gegen den `master`-Branch zu prüfen. Die gesamten Umbauplan-Dokumente wurden also mit `master` als Referenz geschrieben, während die komplette bisherige Umsetzung (Phase 0/1, alle Fixes) auf `main` passiert ist. **Für Adi wichtig, keine KI-Entscheidung:** entweder `main` wird zur neuen Referenz erklärt (dann Doku 00 entsprechend korrigieren) oder auf `master` gewechselt (dann müssten die uncommitteten `main`-Änderungen migriert werden). **Ergänzung (Session: Claude-7fff0345, 2026-07-16, erster echter `git diff` zwischen den Branches):** Differenzierter als bisher angenommen. `git diff main master -- app/lib/domain/signal_processor.dart` ist LEER – Datei identisch, für Phase 2 also branch-irrelevant. `workout_engine.dart` unterscheidet sich, aber NICHT zugunsten von `master`: dort fehlen der komplette Diagnose-Block (`diagEngineSampleCount` u. a.) UND die bereits validierte Tie-Tolerant-Peak-Erkennung (`>=` statt `>`, siehe `ANALYSE_EXTERNE_KI_2026-07-12.md` Punkt F) – `master` hat dort die ältere, nachweislich fehlerhafte Variante. Die "master ist weiter"-Aussage bezieht sich also offenbar nur auf docs/ und pubspec.yaml-Dependencies, nicht auf den Engine-Code – echte Divergenz in beide Richtungen, kein einfaches Gefälle. **AUFGEKLÄRT durch tatsächlichen Fortschritt, nicht durch Analyse (Session: Claude-c00679f3, 2026-07-16/17, `git log`/`git diff` direkt gegen GitHub):** Die Divergenz oben ist inzwischen einseitig aufgelöst. `master`s letzter Commit ist weiterhin `76593fa` (12.07., "HANDOFF fuer naechste KI") – seitdem still. `main` ist seitdem 12 Commits weiter und hat dabei GENAU die damals main-seitig fehlenden `master`-Vorteile übernommen: Firmware-Merge (`e7be63a`, NimBLE + Auto-Start – exakt das, was Claude-449d4d43 per `git checkout master -- firmware/` gestaged hatte), kompletter `docs/Umbauplan Flowrep/`-Ordner inkl. dieser Datei (`6eb885b`), plus alles danach (CSV-Recorder, DSP-Tools, ADR-020, Race-Condition-Fix `3aecd27`, Baseline-Fix `3907706`, jetzt auch Calib-2.0 Paket 1). `git diff main origin/master` zeigt entsprechend nur noch Dateien, die AUSSCHLIESSLICH auf `main` existieren (u. a. diese Datei selbst) oder auf dem längst obsoleten `master`-Stand (`docs/STATUS_REPORT_2026-07-12.md`). **Für zukünftige Sessions:** `main` ist der kanonische Branch, `master` kann als historisch betrachtet werden – bitte nicht mehr als offene Entscheidung führen, es sei denn, Adi entscheidet aktiv anders.
- [x] **Gelesen und ausgewertet (Session: Claude-7fff0345, 2026-07-15):** `docs/00_ENTSCHEIDUNGEN_ERFORDERLICH.md` ist von Adi bereits vollständig ausgefüllt. Kernpunkte: reines Privatprojekt (evtl. später kommerziell), kein fixes Zeit-/Erfolgskriterium, nur Adi selbst als Tester, nur Bizeps-Curls für V1, Korrekturrate-Kriterium bestätigt (auf 1 statt 5 Personen angepasst), Drift bestätigt (an Claude delegiert), kein Zeit-/Budgetdruck, kein Store-Release geplant, Cloud-Sync gewünscht aber erst Phase 5. Löst damit auch die Adi-only-Frage weiter unten in diesem Abschnitt direkt auf.
- [ ] Vollständiger Inhalt von `docs/CALIBRATION_MODE_CONCEPT.md` (bisher nur aus Zitaten bekannt, Datei selbst nur auf `master`, siehe oben)
- [x] **Session Claude-c8b5c268:** `SignalProcessor`-Methodenabgleich gegen `03_CONTRACTS_AND_BLUEPRINTS.md` – zusätzliche Lücke gefunden und behoben: `home_screen.dart` importierte `data/security/calibration_store.dart`, das lokal (Branch `main`) gar nicht existierte (nur auf `master`). Datei von `origin/master` unverändert übernommen, `flutter_secure_storage: ^9.2.2` (Version von `master`s pubspec.yaml übernommen) zu `app/pubspec.yaml` ergänzt. Nur dieses eine Paket ergänzt – NICHT `flutter_blue_plus`/`drift`-Versionen oder den `sqlite3mc`-Hook aus `master`s pubspec.yaml angeglichen, das sind größere, separate Entscheidungen (siehe Abschnitt D unten).
- [ ] Liegt bereits eine echte Mehrfach-Rep-CSV-Aufnahme vor, oder nur der einzelne Curl-Schnipsel aus dem alten Handoff?
- [!] **GEPRÜFT, gravierender Fund (Session: Claude-7fff0345, 2026-07-15):** Mit hoher Wahrscheinlichkeit NEIN. `firmware/src/main.cpp` (aktueller Checkout) implementiert Kommando 0x04 überhaupt nicht – `ControlPointCallbacks::onWrite()` kennt nur 0x01/0x02/0x03, alles andere landet in `default: break;`. Gravierender als die reine 0x04-Frage: diese Datei widerspricht an mehreren Stellen dem Rest der Doku – klassisches `BLEDevice.h`/`BLEServer.h`/`BLE2902` statt NimBLE; `notify()` MIT CCCD-Descriptor (`BLE2902`) statt des dokumentierten No-CCCD-Read-Pollings; **kein Auto-Start des Streamings** (`streaming` bleibt `false`, bis 0x01 empfangen wird – aber `ble_sensor_provider.dart` sendet laut eigenem Kommentar bewusst NIE 0x01, weil es von Auto-Start laut ADR-017 ausgeht). Wäre das wirklich die geflashte Firmware, würde das die '0 Reps'-Symptomatik ziemlich direkt erklären: kein Streaming → Characteristic ändert sich nie → `read()` liefert dauerhaft denselben Wert. Protokoll-Ebene (Skalierung 0.001g/0.01°/s, 52-Byte-Layout) stimmt dagegen exakt mit `01_protocol.yaml` überein – die Diskrepanz betrifft nur die Verbindungs-/Streaming-Logik, nicht das Wire-Format. **Offene Frage an Adi (siehe Chat):** Ist dieses main.cpp wirklich die aktuell geflashte Firmware, oder wurde direkt über die Arduino-IDE/PlatformIO eine neuere Version geflasht, ohne sie zurück in dieses File zu committen? **Definitiv geklärt, mit hartem Beleg direkt vom Gerät (Session: Claude-449d4d43, 2026-07-16):** Nein – main.cpp (main-Branch) ist NICHT die geflashte Firmware. Gerät angeschlossen (COM3, CH9102-USB-Chip), per esptool.py verifiziert: ESP32-PICO-V3-02, 8 MB Flash. App-Partition (0x10000, 1,25 MB) rein lesend ausgelesen (`read_flash`, keine Schreiboperation) und nach Textspuren durchsucht: 34× "NimBLE", 24× "nimble", inkl. wörtlicher Compiler-Pfade wie `.pio/libdeps/m5stick-c-plus2/NimBLE-Arduino/src/NimBLEDevice.cpp" - UND die exakten Log-Strings "CMD: 0x04 received -> dummyStream=%d", "dummy on", "dummy off", "arming IMU....", "gestoppt". Das sind wortgleich die Strings aus **`firmware/src/main.cpp` auf dem `master`-Branch, Commit `ae0354c`** ("BLE Datenpfad geloest + Auto-Stream + IMU-Debug", 2026-07-12) - per `git show master:firmware/src/main.cpp` gegengeprüft, Zeile für Zeile deckungsgleich mit den Flash-Funden (NimBLE-Migration, Auto-Start in onConnect, 0x04-Dummy-Toggle, I2C/BMI270-Diagnostik, Stale-Data-Erkennung, spannungsbasierte Batterieberechnung). **Die geflashte Firmware ist also nicht kaputt oder veraltet - im Gegenteil, sie ist die fortgeschrittene, hardwaregetestete Version.** Das Problem ist rein git-seitig: `master`s main.cpp wurde nie nach `main` gemergt, und `main`s eigene Kopie blieb auf dem allerersten, im eigenen Kopfkommentar als "nicht hardwaregetestet" markierten Entwurf stehen. **Für die main/master-Entscheidung oben relevant:** kein einfaches Gefälle - `master` ist bei Firmware+Docs klar vorn, `main` bei `workout_engine.dart` (siehe Claude-7fff0345s Fund direkt oben). Eine saubere Lösung braucht vermutlich beides: `master`s `firmware/src/main.cpp` nach `main` übernehmen, ohne die dortigen Engine-Verbesserungen zu verlieren. Keine Datei geändert - reiner Lesevorgang am Gerät + Git-Vergleich. Diagnose-Rohdump (1,25 MB) liegt noch unter `C:\Users\adini\Desktop\flowrep_device_dump.bin`, nicht im Repo, kann bei Bedarf gelöscht werden. **NACHTRAG, Merge durchgeführt (Session: Claude-449d4d43, 2026-07-16, auf Adis "mach im Plan weiter"):** `firmware/src/main.cpp` UND `firmware/platformio.ini` per `git checkout master -- <pfad>` von `master` nach `main`s Arbeitsverzeichnis geholt (beide Dateien gehören zusammen: `platformio.ini` deklariert erst dadurch `h2zero/NimBLE-Arduino@^1.4.0`, ohne die main.cpp gar nicht kompilieren würde). Danach **`pio run` tatsächlich ausgeführt: [SUCCESS] in 27,4 s**, NimBLE-Arduino 1.4.3 korrekt aufgelöst, Flash-Nutzung 58,4 % (765 177 von 1 310 720 Bytes) – plausibel im Vergleich zur echten Geräte-Partitionsgröße. Nur eine harmlose Compiler-Warnung (mehrdeutiger `Wire.requestFrom`-Overload, keine Fehlerquelle). Beide Dateien sind jetzt **staged** (`git add`-Äquivalent durch `checkout`), aber **NICHT committet** – Commit-Entscheidung bewusst Adi überlassen, zumal parallel weitere uncommittete Änderungen aus anderen Sessions offen sind (siehe Risiko-Hinweis von Claude-9936160f oben). `main`s Firmware-Quellcode entspricht damit jetzt inhaltlich dem, was nachweislich auf dem Gerät läuft. Sonst keine weiteren Dateien verändert.
- [x] **Beantwortet über `00_ENTSCHEIDUNGEN_ERFORDERLICH.md` (Session: Claude-7fff0345):** Privates Projekt, evtl. später kommerziell. Weitere Übungen nicht dringend (V1 bewusst nur Bizeps-Curls). Erfolgskriterium ist die Korrekturrate aus dem Testprotokoll (< 15 %, 1 Person), nicht separat "8/10 Sätze" formuliert – siehe Datei für Details.
- [x] **Session Claude-c8b5c268:** Doppel-Peak-Übung (erwartet 10, gezählt 20) ein drittes Mal unabhängig bestätigt – diesmal durch tatsächliche Ausführung (nicht Nachvollziehen) in einer separaten Sandbox mit Python-Interpreter. Bestätigt: pre-existing, unabhängig von ADR-020/Settle-Gate, weiterhin ungelöst und nicht Teil dieser beiden Fixes.

## C. Umsetzung (`05_EXECUTION_PLAN.md`)

- [x] erledigt (Session: Claude-2026-07-14-A) – Phase 0: Bestandsaufnahme (ADR-020-Bug im aktuellen `workout_engine.dart`/`home_screen.dart` bestätigt – siehe Änderungsprotokoll)
- [~] **Korrigiert zurück von [x] (Session: Claude-7a8c8856)** – **Phase 1: Bugfix Kalibrierungs-Persistenz (ADR-020).** Die vorherige "[x] erledigt, mehrfach unabhängig verifiziert"-Markierung war verfrüht: alle bisherigen "Verifikationen" (meine eigene, die von Claude-2026-07-14-A, die von Claude-d27f1c13) waren Python-Rekonstruktionen/manuelles Nachvollziehen, NIE echtes `flutter test`. Adis erster echter `flutter test`-Lauf (2026-07-14) fand **2 fehlschlagende Tests**, beide an derselben Stelle: Guided Calibration kann abschließen, WÄHREND der letzte Rep noch abklingt (Peak-Bestaetigung braucht nur 1 Sample nach dem Maximum, nicht die volle Rückkehr zur Ruhe) – das noch erhöhte Signal riss den Zustand sofort wieder aus `idle`. Betraf den BESTEHENDEN Plateau-Test genauso wie den neuen ADR-020-Test, unabhängig vom ADR-020-Fix selbst (mit eigener `UnifiedEngineSim` in `tools/workout_engine_simulation.py` verifiziert: tritt AUCH ohne ADR-020-Fix auf, landet dann nur in `calibrating` statt `active` – beides falsch). **Fix:** neues `_awaitingSettleAfterCalibration`-Gate in `workout_engine.dart` (idle wertet Bewegung erst wieder, sobald das Signal einmal unter `baseline + minThresholdAboveBaseline` war). Python-Gegenstück in `UnifiedEngineSim`/`run_calibration_settle_regression_suite()` ergänzt, kompletter Simulationslauf grün (Exit-Code 0). Dart-Seite NICHT durch echte Kompilierung verifiziert (kein Terminalzugriff) – nur durch Analogieschluss zur jetzt grünen Python-Simulation, die dieselbe Datengenerierung 1:1 nachbildet. **`flutter test` durch Adi MUSS diesmal wirklich die Bestaetigung liefern, nicht nur `flutter analyze`.** **[x] ERLEDIGT, jetzt ECHT verifiziert (Session: Claude-9936160f, 2026-07-15):** Erstmals echten `flutter analyze` + `flutter test` via Desktop Commander (Terminalzugriff auf Adis Rechner) ausgeführt statt nur Python-Analogie. Ein Umgebungsproblem musste dafür gelöst werden: die von Desktop Commander gespawnte PowerShell-Session hatte die Env-Var `ProgramFiles(x86)` nicht gesetzt, wodurch `flutter test` sofort mit nur der Zeile "%PROGRAMFILES(X86)% environment variable not found." abbrach (`flutter analyze` ist davon nicht betroffen, lief problemlos). Fix pro Session: `${env:ProgramFiles(x86)} = 'C:\Program Files (x86)'` vor dem Aufruf setzen (für künftige Sessions notiert, betrifft nur den Desktop-Commander-Prozess, nicht Adis normales Terminal). **Danach: `flutter test` lief vollständig durch – 15/15 Tests grün, "All tests passed!", inklusive exakt des ADR-020-Regressionstests** ("the guided-calibration threshold must NOT be silently overwritten by the old one-rep auto-calibration") **und des bestehenden Plateau-Tests, der vorher (Adis Lauf 2026-07-14) mit 2 Fehlschlägen durchgefallen war.** Der Settle-Gate-Fix (`_awaitingSettleAfterCalibration`) ist damit erstmals durch echte Dart-Kompilierung+-Ausführung bestätigt, nicht nur durch Python-Analogieschluss. `flutter analyze`: weiterhin exakt die 2 bekannten `License`-Fehler in `ble_sensor_provider.dart:128` (siehe Abschnitt D) – nichts Neues, nichts Unerwartetes. **Phase 1 damit nach Definition-of-Done (`flutter analyze` + `flutter test`, Abschnitt 4.4 der Guardrails) inhaltlich fertig; einzige verbleibende Differenz zu 100 %-DoD sind die 2 vorbestehenden, unabhängigen License-Fehler aus Abschnitt D (Dependency-Entscheidung, nicht Teil von ADR-020).**
- [x] **Untersucht, mit klarer Empfehlung (Session: Claude-9936160f, 2026-07-15):** Vor Beginn erst `02_ARCHITECTURE_DECISION_RECORDS.md` (ADR-019), `04_DSP_LABOR_PYTHON_VALIDIERUNG.md` und `RECHERCHE_99_PROZENT_GENAUIGKEIT_2026-07-14.md` gründlich gelesen (wie von Claude-f3e91a7c empfohlen), außerdem geprüft: keine einzige echte CSV-Aufnahme existiert im Repo (0 Treffer bei rekursiver Suche) – die Phase-2-Frage kann also nur synthetisch beantwortet werden. Neues Skript `tools/dsp_lab_phase2_extended.py` geschrieben und ausgeführt (Sandbox + nochmal direkt auf diesem Rechner, identisches Ergebnis): erweitert Dokument 04, Skript 1/1b um die dort selbst als Lücke benannten Effekte Haltungsdrift (4°) und Gyro-Bias (3°/s, Annahmewerte). **Ergebnis: die Lücke zum Komplementärfilter wird dadurch GRÖSSER statt kleiner** (Pause/Peak-Verhältnis 0,26–0,27 vs. aktuell deployte Formel 0,10, statt 0,16 vs. 0,09 im sauberen Fall) – Gyro-Bias trifft den Komplementärfilter besonders hart, weil er das Gyrosignal aufintegriert (klassisches AHRS-Problem). Parameter-Sweep (15 Kombinationen aus α und Trust-Bandbreite) findet bestenfalls ein Unentschieden (α=0,2, aber das unterminiert die eigentliche Idee des Filters – Details in Dokument 04, neuer Abschnitt "Skript 1c"). **Nach ADR-019s eigener Regel ("bleibt schlechter oder gleichauf → einfachere Lösung vorziehen") lautet die Empfehlung: Phase 3 vorerst NICHT beginnen.** Weiterhin nur synthetisch – die eigentlich entscheidende Prüfung (Dokument 07, Szene F/G mit echten CSV-Daten) bleibt aus, weil die dafür nötige Aufnahme-Funktion in der App noch nicht existiert. **Offene Entscheidung für Adi:** (a) trotzdem mit Phase 3 fortfahren, (b) Phase 2/3 zurückstellen und zuerst die CSV-Aufnahmefunktion aus Dokument 07 bauen, oder (c) etwas anderes priorisieren (z. B. den offenen Firmware-Fund unten oder den ENG:/samples-Hardwaretest, die beide grundsätzlicher sind als DSP-Feintuning).
- [x] **Erledigt, smoke-getestet (Session: Claude-7fff0345, 2026-07-16):** `tools/dsp_lab_phase2_real_data.py` geschrieben – deckt Execution-Plan-Aufgaben 2+3 ab (Achsenzuordnung, Pause/Peak alt vs. neu), erkennt Rep-Fenster automatisch aus der `workout_state`-Spalte statt manueller Zeitstempel. **Wichtig:** verwendet das TATSÄCHLICHE Format aus `csv_session_recorder.dart`/Dokument 08 (`accel_x_g`/`gyro_x_dps`/`workout_state`, ohne `filtered_accel_x/y/z`), NICHT das ursprüngliche Dokument-07-Format – beim Nachlesen von Dokument 08 festgestellt, dass Claude-9936160f das bewusst geändert hat (siehe dort Abschnitt 2), sonst hätte das Skript nie zu echten Aufnahmen gepasst. Einheiten-Sanity-Check eingebaut (g/deg-per-s → m/s²/rad/s-Konvertierung wird geprüft, nicht nur angenommen). Smoke-Test mit synthetischer CSV erfolgreich: Sanity-Check OK (9,79 ≈ 9,81), Achsenerkennung korrekt (X dominant), Pause/Peak-Werte (0,093 deployt / 0,164 komplementär im Mittel) reproduzieren die Größenordnung aus Skript 1/1b in Dokument 04 – CSV-Schreiben/Lesen-Rundtrip funktioniert also korrekt. **Noch nicht gegen echte Aufnahmen gelaufen** (die CSV-Funktion ist zwar jetzt fertig, siehe Eintrag direkt unten, aber noch niemand hat tatsächlich aufgenommen). Aufruf: `python tools/dsp_lab_phase2_real_data.py <pfad_zur_csv>`.
- [ ] Phase 3: Dart-Portierung Gravitationskompensation
- [x] **Umsetzung abgeschlossen und verifiziert (Session: Claude-9936160f, 2026-07-15):** CSV-Aufnahmefunktion aus Dokument 07 (Szenen A-G), Plan siehe `08_CSV_AUFNAHME_IMPLEMENTIERUNGSPLAN.md`, von Adi freigegeben. **Neu:** `app/lib/data/repositories/csv_session_recorder.dart` (`CsvSessionRecorder`, unabhängiger zweiter Listener auf `ISensorProvider.samples`, eigene `SignalProcessor()`-Instanz mit Produktions-Defaults, schreibt nach `getExternalStorageDirectory()/recordings/`). **Geändert:** `home_screen.dart` (neue Buttons "Aufnahme starten/stoppen"+"Teilen" hinter `!kReleaseMode && !_isMock`, `_bindEngine()` um dritten, rein additiven Listener ergänzt, `dispose()` erweitert – **beim Debuggen gefunden:** `_refreshTimer` wird bereits für Batterie-Polling verwendet, eigenen `_recordingSampleCountTimer` benutzt statt ihn zu kapern), `pubspec.yaml` (`share_plus: ^12.0.1` – aktuelle Recherche ergab Major-API-Wechsel auf `SharePlus.instance.share(ShareParams(...))`, alte statische API verwendet zu haben hätte nicht kompiliert; nebenbei den toten `STATUS.md`-Verweis aus Abschnitt B auf `STATUS_FORTSCHRITT.md` korrigiert). **Verifiziert, nicht nur behauptet:** `flutter pub get` (share_plus 12.0.2, keine Konflikte), `flutter analyze` → **0 Probleme**, `flutter test` → **15/15 grün** (inkl. des inzwischen von Claude-449d4d43 durchgeführten `flutter_blue_plus`-Bumps – beide Änderungen vertragen sich). **Nicht getestet:** echter Hardware-Lauf – kann nur Adi (App bauen, Aufnahme starten, Curls machen, `Android/data/com.flowrep.flowrep/files/recordings/` prüfen oder Teilen-Button nutzen).
- [ ] Phase 4: End-to-End-Vergleich alt vs. neu auf Hardware
- [ ] Phase 5 (optional): klassischer ML-Klassifikator für mehrere Übungen

## D. Unabhängig von ADR-020/Umbauplan gefunden, noch offen

- [x] **Bestätigt, nicht nur vermutet (Session: Claude-f3e91a7c):** Der `flutter_blue_plus`/`License`-Fehler ist real und die Ursache ist jetzt klar, nicht nur "irgendein Versions-Constraint". Recherche bestaetigt: der `license`/`License`-Parameter in `connect()` existiert erst ab flutter_blue_plus **2.0.0** (Lizenzmodell-Umstellung); `pubspec.yaml` pinnt aber `^1.32.0` (auflösend z.B. zu 1.36.8, wie im flutter-analyze-Fehler zu sehen), wo dieser Parameter/Enum schlicht nicht existiert. Zusaetzlich: die urspruenglichen Handoff-Dokumente (von mir frueher in dieser Konversation gelesen, jetzt aber verschwunden - siehe Abschnitt B) nannten explizit `flutter_blue_plus v2.3.10` als die tatsaechlich hardware-getestete Version. Das ist damit sehr wahrscheinlich keine offene Versions-ENTSCHEIDUNG, sondern eine Regression: pubspec.yaml ist von der bereits validierten 2.x-Version auf eine 1.x-Version zurueckgerutscht. `License.nonprofit` (im Code bereits verwendet) ist fuer Adis privates Projekt die richtige, kostenlose Kategorie. **Trotzdem nicht eigenmaechtig geaendert** (Versions-Bump ist Adis Entscheidung) - aber jetzt mit klarer Empfehlung statt offener Frage.
- [!] **Neu, wichtig (Session: Claude-f3e91a7c):** `ble_sensor_provider.dart` hat einen inneren Widerspruch. Der Klassenkommentar sagt "NOT hardware-tested: written... before the M5StickC Plus2 arrived" - aber derselbe Code enthaelt hochspezifisches, nur durch echtes Hardware-Testen erklaerbares Wissen ("HyperOS ignores client MTU requests and always returns the server's MTU (517 from NimBLEDevice::setMTU(517))", die komplette No-CCCD-Erklaerung). Referenzen im selben File (`docs/protocol.yaml`, `docs/adr/ARCHITECTURE_DECISION_RECORDS.md`) passen zudem nicht zu den echten Pfaden (`docs/01_protocol.yaml`, `docs/04_ARCHITECTURE_DECISION_RECORDS.md`). Vermutung: Diese Datei wurde von einer Instanz ueberschrieben/neu erzeugt, die die inzwischen verschwundenen historischen Handoffs (Abschnitt B) nicht mehr kannte, und der Kommentar-Kopf ist dadurch nicht mehr akkurat. Nicht selbst korrigiert - haengt eng mit der ungeklaerten Dateien-Frage in Abschnitt B zusammen.
- [!] **Neu (Session: Claude-7a8c8856):** `flutter analyze`, korrekt aus `app/` ausgeführt, findet 2 echte Fehler in `lib\data\providers\ble_sensor_provider.dart:128`: `license`-Parameter/`License`-Typ undefiniert. Vermutliche Ursache: `pubspec.yaml`-Constraint löst `flutter_blue_plus 1.36.8` auf, der Code scheint aber gegen die API von `2.3.10` geschrieben (dort gibt es laut früheren Handoff-Notizen einen `License`-Parameter, z. B. `License.nonprofit`). Nicht angefasst – Versions-Entscheidung (Dependency hochziehen vs. Code an 1.36.8 anpassen) sollte Adi treffen, nicht eigenmächtig eine KI. Blockiert "`flutter analyze` fehlerfrei" als DoD-Kriterium, unabhängig von allem oben. **[x] BEHOBEN UND VERIFIZIERT (Session: Claude-449d4d43, 2026-07-16, auf Adis explizites "ja bitte" hin):** Vor der Änderung nochmal gegengeprüft, dass `pubspec.yaml` seit meinem letzten Blick unverändert war (war es). `flutter_blue_plus: ^1.32.0` → `^2.3.10` in `app/pubspec.yaml` gesetzt (Version + erklärender Kommentar 1:1 von `master` übernommen, `ble_sensor_provider.dart` selbst ist auf beiden Branches identisch – die Datei war nie das Problem, nur die Dependency-Version). Danach vollständig verifiziert, nicht nur angenommen: `flutter pub get` (bestätigt `flutter_blue_plus 2.3.10 (was 1.36.8)`), `flutter analyze` (**0 Fehler**, nur noch 4 Warnungen in `home_screen.dart` zu unbenutzten Feldern/Methoden – erkennbar Teil der laufenden CSV-Aufnahme-Arbeit einer anderen Session, nicht angefasst), `flutter test` (**15/15 weiterhin grün, Exit-Code 0** – der Versionssprung 1.x→2.x hat nichts an der Engine-Testsuite kaputt gemacht). Der `License`-Fehler, der seit Claude-7a8c8856s Session (mehrere Tage/Sessions) offenstand, ist damit vollständig aufgelöst. `pubspec.lock` wurde durch `pub get` aktualisiert (war/ist untracked, siehe Abschnitt B). Nicht committet, wie beim Firmware-Merge – liegt zusammen mit den anderen offenen Änderungen bereit für Adis Commit-Entscheidung.

---

## Änderungsprotokoll (neueste zuerst)

- **2026-07-19, Agent-1-SignalPipeline (Claude sandbox session Claude-c00679f3), Branch `agent1-schritt-c-und-b`, noch NICHT gemergt:** Auf Adis Anweisung Schritt C und Schritt B nacheinander umgesetzt. **Schritt C:** vor dem Anfassen erst geprüft, was Agent 4s Protokoll-v2 bereits erledigt hat - die App-seitige Zeitstempel-Synthese (`i*20ms`) ist jetzt per Firmware-Garantie ehrlich, laut `docs/01_protocol.yaml` explizit KEINE Änderung nötig. Verbleibender Schritt-C-Umfang war also nur noch S5 (stiller Datenverlust): Dedup-Logik aus `ble_sensor_provider.dart` in eine reine, testbare `BatchDedupTracker`-Klasse extrahiert, die Duplikate UND Lücken (wahrscheinlich verpasste Batches) jetzt zählt statt sie stillschweigend zu verwerfen. **Schritt B:** g_p (vorzeichenbehaftete Gyro-Projektion) als Opt-in-Schattenzähler (`useSignedProjectionCounting`, Default `false`) in `signal_processor.dart`/`workout_engine.dart` ergänzt - läuft parallel zum bestehenden, hardware-verifizierten Combined-Signal-Pfad, ohne ihn zu verändern (eigener Isolations-Test bestätigt das). Dominante Achse: Varianz-Heuristik aus der Python-Simulation (Agent 2s `CalibrationController` war zu dem Zeitpunkt noch nicht gemerged). Dabei zwei echte, nur durch `flutter test` gefundene Fehler unterwegs behoben (Reihenfolge-Problem zwischen Achsen-Lernfenster und Kalibrierungs-Timing; zu strenge Test-Toleranz) - Details in den Commit-Messages. **Nach dem Mergen von main (Agent 2 + Agent 3 sind inzwischen ebenfalls gelandet) erneut komplett verifiziert:** `flutter test` 49/49, `flutter analyze` auf allen eigenen Dateien 0 Probleme, die 6 vorbestehenden `drift_database.g.dart`-Fehler unverändert (Agent 3, nicht meins). **NICHT selbst gemergt** - warte auf Adis Entscheidung, wie beim letzten Mal.

- **2026-07-17, Claude-c00679f3 (Gatekeeper-Merge auf Adis explizite Anweisung im Chat, nicht als Agent 1 selbst - siehe 00_KOORDINATION_UEBERSICHT.md Abschnitt 3):** `agent1-signal-pipeline` nach `main` gemergt (`06e0eb5`), NACHDEM auf Adis echtem Rechner via Desktop Commander verifiziert wurde: `flutter test` 19/19 (dann 21/21 nach dem Merge mit dem inzwischen ebenfalls gelandeten Agent-4-Branch), `flutter analyze` auf den eigenen Dateien 0 Probleme. Dabei zwei echte, nur durch echtes Testen auffindbare Fehler gefunden und noch vor dem Merge korrigiert: (1) ein verirrtes `engine.dispose()` außerhalb seines Scopes (reiner Tippfehler), (2) eine ernsthafte Regression - die ursprüngliche Sperrzeit (40 Samples) brach drei bestehende Tests, die sich auf eine 35-Samples/Rep-Kadenz verlassen; korrigiert auf 24 Samples und ehrlich als Rauschschutz statt als vollständigen S1-Fix umbenannt (Details im Merge-Commit). Verbleibend, nicht mein Bereich: 6 vorbestehende `drift_database.g.dart`-Fehler (Namenskollision Domain-Models vs. Drift-generierte Klassen, Agent 3s Baustelle, unverändert seit vor diesem Merge). Agent 4s Branch war zu diesem Zeitpunkt bereits reviewt und gemerged (PR #2) - `docs/01_protocol.yaml` hat jetzt eine echte Protokollversion, Schritt C (App-seitige Zeitbasis-Ehrlichkeit) ist damit für eine künftige Session entsperrt.

- **2026-07-17, Agent-1-SignalPipeline (Claude sandbox session Claude-c00679f3, dieselbe Sandbox wie der Claude-c00679f3-Eintrag darunter, jetzt unter dem Bauplan `AGENT_1_SIGNAL_PIPELINE.md`):** Adi hat mich angewiesen, nach dem P1.1-Patch unten die formale Agent-1-Rolle zu übernehmen. **Wichtige Selbstkorrektur zuerst:** mein eigener P1.1-Patch unten (Commit `db206cd`... nein, Duration-basiert, Commits `db206cd`/`a5e8aee` diese Session davor) entsprach NICHT der Bauplan-Vorgabe (Sperrzeit in Sample-Anzahl, nicht Echtzeit-ms, wegen S3) und deckte S2 gar nicht ab – bewusst durch den vollständigen, bauplankonformen Mechanismus ersetzt statt beide nebeneinander stehen zu lassen (siehe Commits unten).

  **Vorbereitung:** frisches `git pull origin main`, `git log` bestätigt bereits Bekanntes (Settle-Gate/ADR-020, `flutter_blue_plus`-Bump, NimBLE-Merge – alles vorhanden). Zusätzlich `docs/agent-blueprints-2026-07-17`-Branch geprüft (Agent-2- und Agent-4-Bauplan gelesen): Agent 2 wartet explizit auf den Merge dieses Branches, bevor er `workout_engine.dart` anfasst (nur im `calibrating`-Zweig, kein Überlapp mit meinem `_detectPeak()`-Bereich). `docs/01_protocol.yaml` hat noch keine neue Version (Gyro-Skalierung weiterhin `0.01`) → **Schritt C bleibt zurückgestellt**, wie im Bauplan für diesen Fall vorgesehen.

  **Schritt A (P1, S1+S2):** neue Funktion `sweep_live_pfad_refraktaer_und_prominenz()` in `tools/workout_engine_simulation.py`, nutzt `make_persona_6achsen`/`kandidaten_signale`/`zaehle_edge` aus Paket 1 unverändert wieder (nicht dupliziert). Kalibriert einmalig auf einer clean-Rep (Formel 1:1 aus `workout_engine.dart` übernommen), sweept dann Sperrzeit in SAMPLES (nicht Sekunden – S3) gegen alle 5 Personas: **40 Samples (800ms@50Hz)** ist der kleinste Kandidat, der `double_bump` exakt löst (20→10, nicht nur 20→11 wie mein früherer Zeit-Patch), ohne andere Personas zu stören. `falling_debounce=4` unverändert aus `zaehle_edge` übernommen (deren eigener 2026-07-16-Debugfund gegen slow/weak), `prominence_ratio=0.30` analog zu `falling_edge_ratio` gewählt. **S2** (adaptive Schwelle, `adaptive_threshold_ratio=0.25`, ab 3 bestätigten Reps): löst den Tempo-Drift-INNERHALB-eines-Satzes-Fall (3/10→6/10 in eigenem Test), löst NICHT den Kaltstart-Fall (weak/slow komplett unter clean-kalibriertem theta: 0/10, strukturell unerreichbar ohne neue Kalibrierungsdaten – das ist Calib-2.0/Agent 2s Aufgabe, hier bewusst dokumentiert statt stillschweigend als Erfolg verbucht). **Beim Bauen selbst gefunden und behoben, nicht wegsimuliert:** die adaptive Schwelle kann ohne Boden unter die Baseline rutschen und `above_threshold` faktisch dauerhaft wahr halten – Regression bei `make_slow_reps` (8/8→3/8) gefunden und mit einem Boden (`baseline + max(0.10, 0.15×kalibrierte Exkursion)`, analog zu `minThresholdAboveBaseline` in `workout_engine.dart`) behoben.

  **Schritt B (P2, S8):** neue Funktion `pruefe_strukturellen_gp_fix()`, nutzt `kandidaten_signale`s `g_p`-Ausgabe unverändert. Beweis: vorzeichenbasierte Zählung auf `g_p` (nur positiv kreuzende Exkursionen) zählt `double_bump` **10/10 OHNE JEDE Sperrzeit** – strukturell gelöst, nicht durch Schritt A kaschiert (`combined` bleibt unter identischer Bedingung bei 19/10). Achse: geschätzte dominante Gyro-Varianz-Achse (Platzhalter, cos=0,976 zur echten Persona-Achse in diesem Test), nicht Agent 2s spätere PCA-Achse. ZUPT explizit NICHT umgesetzt, weil nicht nötig – reine Peak-/Vorzeichen-Erkennung ohne Integration, dokumentiert statt weggelassen.

  **Dart-Port:** NUR Schritt A (`workout_engine.dart`: `minRepIntervalSamples`/`fallingDebounce`/`prominenceRatio`/`adaptiveThresholdRatio` ersetzen den Duration-Patch vollständig, alle Reset-Punkte konsistent erweitert, `diagEngineSampleCount` als Sample-Zähler wiederverwendet statt dupliziert; `workout_engine_test.dart` entsprechend angepasst + neuer Adaptive-Schwelle-Test). **Schritt B bewusst NICHT nach Dart portiert** – eigene, spätere Runde, nicht an einen bereits umfangreichen Schritt-A-Port drangehängt.

  **Ehrlich zur Definition of Done (Bauplan Abschnitt 7):** `python tools/workout_engine_simulation.py` läuft durch (Exit 0), Alt-Suite 5/5, Paket-1-Suite unberührt (5/5) – **erfüllt**. `flutter analyze`/`flutter test` **kann ich nicht ausführen** (kein Flutter/Dart-SDK in dieser Sandbox, kein `pub.dev`-Netzwerkzugriff) – bitte vor jedem Merge nachholen. `git diff --stat` gegen `main` zeigt nur die drei erlaubten Dateien aus Abschnitt 4. Kein `git push origin main` ausgeführt.

  **Git:** `agent1-signal-pipeline`-Branch von `main` (nach dem P1.1-Patch unten), zwei Commits (`2343380` Python, `0a8c0ff` Dart), Branch gepusht, **main nicht gemerged** – wartet auf Adis/Lead-Claudes Review. Dieser Statusbericht selbst geht direkt auf `main` (frisch gepullt, nur angehängt), nicht in den Branch – reine Koordinations-Sichtbarkeit für Agent 2/3/4, kein Code, kein Kollisionsrisiko mit deren Dateien.

  **Für Agent 2, wenn der Merge passiert ist:** `_detectPeak()` hat jetzt mehr Zustand als vorher (`_confirmedPeaks`, `_lastCountedRepSample`, `_preMin`, `_excursionFallingThreshold`) – alles privat zu `WorkoutEngine`, sollte euren `calibrating`-Hook nicht betreffen, aber bitte selbst gegenlesen statt nur diesem Satz zu vertrauen.

- **2026-07-17, Claude-c00679f3 (separate Konversation mit Adi, nur claude.ai-Sandbox – kein Desktop Commander, kein Flutter/Dart-SDK verfügbar):** Auf Adis Anweisung zuerst GitHub frisch geprüft statt vom letzten eigenen Stand auszugehen: `d76ebbb` (Paket 1, Known-Count-Optimierer + 5 Personas) war bereits gepusht, `0acbdf6` (kleiner Namespace-Fix in `drift_database.dart`) dazu. Paket-1-Suite selbst ausgeführt statt nur gelesen: `python3 tools/workout_engine_simulation.py`, Exit-Code 0, alle 5 Personas B 5/5, C 3/3, Validierungs-MAE ≤ 0,5 wie im Commit behauptet. **Adi hat Paket 2 (`CalibrationController`, `ExerciseProfile`, Store-Migration v1→v2) explizit einer parallelen Session zugewiesen – bewusst nicht angefasst:** keine Änderung an `calibration_store.dart`, kein neues `ExerciseProfile`-Modell, keine neue `calibration_controller.dart`. **Eigener Beitrag: P1.1 (Refractory/Lockout im Live-Pfad, Fix S1, RECHERCHE_ZAEHLROBUSTHEIT_2026-07-16.md §3)** – bewusst als eigenständiger, kleiner Patch NEBEN Paket 1/2, nicht als Teil davon (Begründung im Chat mit Adi: der Alt-Pfad `_detectPeak()`/`WorkoutEngineSim._detect_peak()` bleibt bis Paket 4 (Engine-Anbindung) produktiv, profitiert also unabhängig vom Calib-2.0-Fortschritt). Python-first (ADR-022): `min_rep_interval_s` (Default 0,8 s, fester Wert – Begründung im Code-Kommentar: aus `reps_in_set`-Timing selbst gelernt wäre während des aktiven Bugs kontaminiert, siehe `workout_engine.dart`-Kommentar zu `minRepInterval`) in `WorkoutEngineSim._detect_peak()` ergänzt, Alt-Suite lief danach unverändert durch (Exit-Code 0): Doppel-Peak-Übung **20 → 11 gezaehlt (erwartet 10)**, keine Regression bei den übrigen Szenarien (Zittrige Kalibrierung sogar 11→10 verbessert). Erst danach 1:1 nach `workout_engine.dart` portiert (`minRepInterval`-Konstruktorparameter + `_lastCountedRepAt`, konsistent in allen bestehenden Full-Resets ergänzt: `handleReconnect()`, `applyCalibration()`, `startGuidedCalibration()` – NICHT in `_endSet()`, absichtlich, siehe Feld-Kommentar) und ein neuer Dart-Regressionstest in `workout_engine_test.dart` ergänzt (doppelhöckriges Signal, vergleicht Zählung mit/ohne Lockout auf identischem Input). **Ehrlich, nicht nur behauptet:** Diese Sandbox hat kein Flutter/Dart-SDK (kein `pub.dev`-Netzwerkzugriff) – der Dart-Teil ist sorgfältig gegen den 1:1-Python-Fix gegengelesen, aber NICHT selbst mit `flutter test` ausgeführt. Bitte bei nächster Gelegenheit mit echtem Flutter-Zugriff verifizieren (`flutter test`, insbesondere der neue Test in `workout_engine_test.dart`). **Nebenbei, kollisionsarm (andere Dateien als Paket 1/2):** `main`-vs-`master`-Frage in Abschnitt B unten als durch tatsächlichen Fortschritt aufgelöst ergänzt (nicht überschrieben – Zusatz am Ende des bestehenden Eintrags); `tools/__pycache__/*.pyc` aus dem Tracking entfernt (war trotz `.gitignore`-Regel bereits getrackt); README.md mit Hinweis versehen, dass die Status-Tabelle dort veraltet ist und auf dieses Dokument verweist, Tabelle selbst nicht neu geschrieben. Alles committet und gepusht (siehe Commit-Historie); Token für den Push war der bereits im Chat mit Adi im Klartext geteilte – sollte weiterhin zeitnah rotiert werden.

- **2026-07-16, Claude-7da3624e (separate Cloud-Sandbox-Konversation mit Adi – erst mit dieser Nachricht zum ersten Mal per Desktop Commander mit diesem lokalen Ordner verbunden):** Kam aus einer parallelen Konversation, in der Adi mir das Umbauplan-Set hochgeladen hatte; dort nur gegen den *Remote*-Stand geprüft (`git fetch` auf `origin/main`/`origin/master`, kein lokaler Zugriff) und dabei unabhängig einen sehr ähnlichen `hasValidCalibration`-Fix entworfen, in eigener Python-Rekonstruktion verifiziert (Bug reproduziert: Threshold sprang 1.65→5.03; mit Fix unverändert) – bemerkenswert nah an der hier bereits echten Umsetzung, nur als Konstruktor-Flag statt aus `initialPeakThreshold` abgeleitet. **Ehrlich festgehalten:** Meine Rekonstruktion hätte das Settle-Gate-Problem NICHT gefunden – exakt das Muster, das dieses Dokument mehrfach beschreibt (Analogieschluss vs. echte Ausführung). Nach Erhalt des Desktop-Commander-Zugriffs alles oben Behauptete unabhängig frisch nachgeprüft statt nur gelesen: `flutter analyze` (0 Probleme, bestätigt), `flutter test` (15/15 grün, bestätigt – inkl. exakt desselben `PROGRAMFILES(X86)`-Workarounds, den ich unabhängig selbst gefunden hatte, bevor ich diesen Eintrag las), `git diff` für `workout_engine.dart` (hasValidCalibration + Settle-Gate wie beschrieben), `home_screen.dart` (CSV-Recorder, eigener `_recordingSampleCountTimer`, `SharePlus.instance.share`), `pubspec.yaml` (flutter_blue_plus 2.3.10, flutter_secure_storage), Firmware-Diff (staged, NimBLE bestätigt). **Eigener, neuer Fund:** `git remote -v` zeigte den GitHub-Token im Klartext in der Remote-URL – Verstoß gegen die eigene Guardrail 6.5. Sofort behoben (`git remote set-url origin https://github.com/Adilinu94/flowrep.git`), sonst nichts geändert. Nichts committet – Vorschlag dazu im Chat mit Adi.

- **2026-07-16, Claude-7fff0345 (Abschluss dieser Sitzung):** Nach Fertigstellung von `tools/dsp_lab_phase2_real_data.py` die Datei frisch neu gelesen (wie vorgeschrieben) und dabei festgestellt, dass zwischenzeitlich sehr viel passiert ist: Claude-9936160f hat die CSV-Aufnahmefunktion tatsächlich fertig implementiert (nicht nur geplant), Claude-449d4d43 hat die Firmware-Frage per echtem Geräte-Flash-Auslesen endgültig geklärt und gemergt sowie den `flutter_blue_plus`-Bug behoben. Zur Sicherheit selbst frisch verifiziert statt nur übernommen: `flutter analyze` → keine Probleme, `flutter test` → 15/15 grün, beides nach allen drei parallelen Änderungssätzen. Eigener Beitrag: `tools/dsp_lab_phase2_real_data.py` (Aufgaben 2+3 des Execution Plans), smoke-getestet gegen synthetische Daten, bereit für echte Aufnahmen sobald vorhanden – Details im Abschnitt-C-Eintrag direkt oben. CSV-Aufnahmefunktion selbst nicht angefasst (fremde Arbeit). **Gesamtbild am Ende dieser Sitzung:** so gut wie alles technisch Blockierende ist gelöst oder hat einen klaren nächsten Schritt; die größten verbleibenden Punkte sind rein organisatorisch (Commit-Entscheidung für die diversen uncommitteten Änderungen) bzw. brauchen Adis physisches Zutun (echte CSV-Aufnahmen, ENG:/samples-Hardwaretest).

- **2026-07-15/16, Claude-9936160f (Abschluss):** Nach Freigabe durch Adi (App-Speicher + Teilen-Button, `workout_state`-Spalte, direkt umsetzen) die im Plandokument 08 beschriebene CSV-Aufnahmefunktion tatsächlich implementiert: neue Datei `csv_session_recorder.dart`, Änderungen an `home_screen.dart` und `pubspec.yaml` (Details Abschnitt C). Dabei einen Kollisionsfehler selbst gefunden und vermieden (`_refreshTimer` war bereits durch Batterie-Polling belegt). Vor dem Schreiben dieses Eintrags die Datei neu gelesen (wie in den Regeln vorgeschrieben) und dabei festgestellt, dass in der Zwischenzeit – in einer anderen, parallelen Konversation mit Adi direkt – erhebliche Fortschritte passiert sind: die Firmware-Frage aus Abschnitt B wurde durch echtes, rein lesendes Auslesen des Geräteflashs (esptool.py, COM3) endgültig geklärt (geflashte Firmware = `master`s fortgeschrittene Version, nicht `main`s veralteter Entwurf) und der `firmware/`-Ordner entsprechend gemergt (staged, nicht committet); der `flutter_blue_plus`-Fehler aus Abschnitt D wurde mit Adis Zustimmung behoben und real verifiziert (0 Fehler, 15/15 Tests). Beides von Claude-449d4d43, nicht von mir – nicht dupliziert, nur zur Kenntnis genommen und in meiner eigenen Verifikation (mein `flutter analyze`/`flutter test`-Lauf oben in Abschnitt C) mitbestätigt. **Gesamtstand nach dieser Session:** Phase 0/1 fertig und doppelt real-verifiziert, License-Fehler behoben, Firmware-Quelle stimmt jetzt mit dem Gerät überein (alles außer den Firmware-Dateien noch uncommittet – siehe Risiko-Hinweis oben), Phase 2 synthetisch untersucht (Empfehlung: Phase 3 vorerst zurückstellen), CSV-Aufnahmefunktion fertig und verifiziert, aber noch nicht am echten Gerät getestet. Größte verbleibende offene Punkte: der ENG:/samples-Hardwaretest (Abschnitt B, höchste Priorität, nur Adi kann ihn machen) und die Commit-Entscheidung für die diversen uncommitteten Änderungen.

- **2026-07-16, Claude-449d4d43 (dritte Fortsetzung – Adi: "Ja bitte nochmal prüfen und dann weiter machen"):** Vor der Änderung `git status`/Dateiinhalt von `app/pubspec.yaml` gegengeprüft (unverändert seit meinem letzten Blick, keine Kollision). `flutter_blue_plus` von `^1.32.0` auf `^2.3.10` gehoben (Version + Kommentar von `master` übernommen), danach vollständig verifiziert statt nur angenommen: `flutter pub get` (Downgrade→Upgrade bestätigt: 2.3.10 war 1.36.8), `flutter analyze` (**0 Fehler**, nur noch 4 harmlose, erkennbar fremde Warnungen in `home_screen.dart`), `flutter test` (**15/15 weiterhin grün**). Der seit Claude-7a8c8856 offene `License`-Fehler (Abschnitt D) ist damit behoben. Wie beim Firmware-Merge bewusst nicht committet. Details in Abschnitt D.

- **2026-07-16, Claude-449d4d43 (zweite Fortsetzung – Adi: "mach im Plan weiter"):** Firmware-Merge durchgeführt, den ich im Chat als Option angeboten hatte: `firmware/src/main.cpp` + `firmware/platformio.ini` per `git checkout master -- <pfad>` nach `main` geholt (beide gehören zusammen wegen der NimBLE-Arduino-Abhängigkeit). Danach zur Verifikation tatsächlich `pio run` ausgeführt statt nur anzunehmen, dass es passt – **[SUCCESS]**, NimBLE-Arduino 1.4.3 korrekt aufgelöst, nur eine harmlose Warnung. Bewusst NICHT committet (nur `git checkout`, das staged), da mehrere andere uncommittete Änderungen aus parallelen Sessions offen sind und der Commit-Schnitt Adis Entscheidung ist. Details in Abschnitt B (Firmware-Eintrag, Nachtrag). Sonst nichts angefasst – insbesondere nicht die laufende Phase-2-Arbeit (CSV-Aufnahme, DSP-Analyse) der parallelen Sessions.

- **2026-07-16, Claude-449d4d43 (Fortsetzung derselben Session, jetzt mit Adi direkt im Chat):** Adi hat im Chat direkt nach Punkt 1 gefragt ("wie kannst du das lösen") und seine Hardware genannt (M5StickC Plus2, ESP32-PICO-V3.0). Gerät war zunächst nicht per USB verbunden (kein COM-Port, keine passende PnP-Geraeteklasse) – nach Anschluss durch Adi gefunden: COM3, CH9102-USB-Chip. Mit PlatformIOs gebündeltem `esptool.py` direkt und ausschließlich lesend am Gerät gearbeitet: `flash_id` (Chip-Verifikation: ESP32-PICO-V3-02, 8 MB) und `read_flash` der kompletten App-Partition (1,25 MB) in eine lokale Datei. Textsuche im Rohdump ergab eindeutigen Beleg, dass die tatsächlich geflashte Firmware NICHT `main`s `firmware/src/main.cpp` ist, sondern `master`s Version (Commit `ae0354c`) – Details, Belege und Konsequenzen für die main/master-Frage in Abschnitt B (dortiger Eintrag direkt ergänzt, nicht dupliziert). Kurze, ergebnislose Suche nach einer moeglichen lokalen Kopie des echten Firmware-Quellcodes in Documents\Arduino, Desktop, Downloads, Documents (fuer den Fall, dass er nur lokal und nicht in Git existiert) – war am Ende ohnehin unnötig, da der Code vollständig auf `master` vorliegt. **Zur Kenntnis genommen, nicht dupliziert oder fortgesetzt:** laut Claude-7fff0345s Eintrag unten hat Adi in einer anderen, parallel laufenden Konversation Phase 1 bereits abgenommen und Phase 2 (CSV-Aufnahme, DSP-Analyse) gestartet – diese Arbeit wird in dieser Session nicht wiederholt, siehe auch Chat-Antwort an Adi.

- **2026-07-16, Claude-7fff0345 (Fortsetzung derselben Konversation – jetzt mit Desktop Commander, vorher nur Filesystem-MCP):** Adi hat Phase 1 abgenommen und Phase 2 angewiesen ("falls unklar, erst recherchieren"). Vor Codeaenderung Dokument 05 (Execution Plan) und Dokument 07 (Datenakquisition) gelesen, dabei entdeckt: (a) `tools/dsp_lab_phase2_extended.py` existierte bereits (Skript 1c), aber ohne Eintrag hier – zwischenzeitlich hat Claude-9936160f das selbst nachgetragen (siehe deren Eintrag unten), keine Duplizierung meinerseits noetig. (b) Mit erstmaligem echten Git-Zugriff (Desktop Commander) `git diff main master` fuer die zentralen Dart-Dateien gegengeprueft: `signal_processor.dart` identisch auf beiden Branches, `workout_engine.dart` dagegen mit `master` im Nachteil (fehlender Diagnose-Block + fehlender Tie-Tolerant-Peak-Fix) – relativiert die bisherige "master ist weiter"-Annahme, Details in Abschnitt B. (c) Laut Document 05 haengen ALLE VIER Phase-2-Aufgaben an echten CSV-Aufnahmen (Aufgabe 1, die Adi explizit einer anderen Session zugewiesen hat). Um trotzdem etwas Nicht-Redundantes beizutragen: `tools/dsp_lab_phase2_real_data.py` in Arbeit – Analyse-Skript fuer Aufgaben 2+3 (reale CSV einlesen, Achsenzuordnung pruefen), das lauffaehig sein soll, sobald die parallel gebaute Aufnahmefunktion Daten liefert. CSV-Aufnahmefunktion selbst NICHT angefasst (fremdes `[~]`, siehe Regel 3 oben). Bemerkenswert: mindestens zwei weitere Claude-Sessions (Claude-9936160f, Claude-449d4d43) haben diese Datei erkennbar zeitgleich mit dieser Sitzung bearbeitet – alle Aenderungen unten wurden vor meinem Schreibvorgang neu gelesen, um Kollisionen zu vermeiden.

- **2026-07-15, Claude-9936160f:** Session mit `STATUS_FORTSCHRITT.md` (Upload durch Adi) und `00_UEBERBLICK` begonnen, wie vorgeschrieben. Hatte als (soweit hier erkennbar) erste Session sowohl Filesystem-MCP als auch Desktop Commander (echter Terminalzugriff) gleichzeitig zur Verfügung. **Drei Hauptbeiträge:** (1) Missing-Files-/main-vs-master-Rätsel aus Abschnitt B mit hartem `git ls-tree`-Beleg geklärt (main: 13, master: 24 Dateien unter docs/) plus Hinweis auf ungetrackten Umbauplan-Ordner und uncommittete Fixes als Datenverlustrisiko – Details siehe Abschnitt B. (2) Phase 1 erstmals durch echten `flutter test` bestätigt (15/15 grün, inkl. ADR-020-Regressionstest) – dafür musste ein `ProgramFiles(x86)`-Umgebungslücke im Desktop-Commander-Prozess umgangen werden (`${env:ProgramFiles(x86)} = 'C:\Program Files (x86)'` vor jedem `flutter`-Aufruf setzen – für künftige Sessions mit Desktop Commander relevant, betrifft nicht Adis normales Terminal). Adi hat das im Chat bestätigt. (3) Auf Adis Anweisung ("Phase 1 ist durch, fange mit Phase 2 an, bei Unklarheit erst recherchieren") Phase 2 begonnen: vor Beginn ADR-019, Dokument 04 und das RECHERCHE_99-Dokument gelesen (Empfehlung von Claude-f3e91a7c befolgt), dabei festgestellt, dass Phase 2 laut ADR-019 explizit daran gebunden ist, dass der Komplementärfilter gegen die AKTUELL DEPLOYTE Formel gewinnt. Keine echten CSV-Daten vorhanden (0 Treffer bei rekursiver Suche im gesamten Repo) – daher nur synthetisch prüfbar. Neues Skript `tools/dsp_lab_phase2_extended.py` (Sandbox + auf diesem Rechner ausgeführt, identisches Ergebnis) erweitert Dokument 04 um Haltungsdrift und Gyro-Bias – beide vergrößern die Lücke zugunsten der aktuell deployten Formel statt sie zu schließen; Parameter-Sweep findet bestenfalls ein Unentschieden mit fragwürdigem Tradeoff. Empfehlung dokumentiert (Dokument 04, neuer Abschnitt "Skript 1c", plus Abschnitt C oben): Phase 3 vorerst nicht beginnen, stattdessen Adi entscheiden lassen zwischen Phase-3-Fortsetzung trotz Befund, CSV-Aufnahmefunktion (Dokument 07) zuerst bauen, oder anderes priorisieren (Firmware-Fund/ENG-Test unten sind grundsätzlicher). Während dieser Session liefen erkennbar mindestens zwei weitere, unabhängige Sessions parallel auf derselben Datei (Claude-449d4d43, Claude-7fff0345, siehe deren eigene Einträge unten) – keine Kollision meiner eigenen Änderungen festgestellt, nur zeitversetzt dieselbe Datei gelesen/geschrieben. Keine der in Abschnitt B/D offenen, nur-von-Adi-zu-treffenden Entscheidungen eigenmächtig getroffen (main/master, flutter_blue_plus-Version, Firmware-Frage, Commit der uncommitteten Änderungen).

- **2026-07-15, Claude-449d4d43:** Session gestartet mit `STATUS_FORTSCHRITT.md` (Upload durch Adi) und `00_UEBERBLICK`, wie vorgeschrieben. Hatte zunächst Desktop-Commander-Zugriff (echtes Terminal): `git status` + `git ls-tree HEAD vs. master -- docs/` unabhängig nachvollzogen – bestätigt exakt Claude-9936160fs Zahlen (13 vs. 24 Dateien, identische Untracked-/Modified-Liste). Die zu dem Zeitpunkt unterbrochene Phase-1-Verifikation (Claude-9936160f, noch ohne eingetragenes Ergebnis) selbst zu Ende geführt: `flutter analyze` (2 bekannte License-Fehler) und `flutter test` nach identischem `PROGRAMFILES(X86)`-Workaround – **15/15 Tests grün, Exit-Code 0**, exakt dieselben Zahlen, die jetzt unter Claude-9936160fs Eintrag stehen. **Wichtiger Prozess-Hinweis für Adi:** Bevor ich mein eigenes Ergebnis eintragen konnte, verlor diese Session den Desktop-Commander-Zugriff (Tool nicht mehr auffindbar, nur noch Filesystem-MCP übrig). Als ich die Datei danach erneut las, stand Claude-9936160fs Ergebnis bereits eingetragen UND ein komplett neuer Eintrag (Claude-7fff0345, samt wichtigem Firmware-Fund, siehe unten) war bereits da – ohne dass ich das geschrieben hatte. Das spricht für eine echte, zeitgleich zu dieser hier laufende weitere Claude-Sitzung auf genau dieser Datei; die genaue Ursache (mehrere offene Chats? etwas anderes?) kann ich von hier aus nicht feststellen, aber Adi sollte das wissen, falls Bearbeitungen mal kollidieren. Deshalb nichts dupliziert, nur unabhängig gegengeprüft. **Eigener Beitrag über reine Bestätigung hinaus:** `firmware/src/main.cpp` nach Claude-7fff0345s Fund selbst gelesen (per Filesystem-MCP) – alle drei Behauptungen bestätigt: klassisches `BLEDevice.h`/`BLEServer.h`/`BLE2902` (kein NimBLE-Include in der Datei), `notify()` MIT `BLE2902`-CCCD-Descriptor, `streaming` startet `false` und wird nirgends automatisch `true` außer durch 0x01. Zusätzlich bemerkt: Der Datei-Kopfkommentar sagt wörtlich "NOT hardware-verified. Written in Claude.ai before the board physically arrived" – dasselbe Muster wie die in Abschnitt D dokumentierte Diskrepanz bei `ble_sensor_provider.dart`. Beide Dateien tragen also denselben "vor Hardware-Ankunft"-Kopfkommentar, während der Rest der Doku sehr spezifisches, nur durch echtes Hardwaretesten erklärbares Wissen referenziert (NimBLE-MTU-Verhalten, ~73,6 Hz Datenrate). **Naheliegende, unbewiesene Hypothese:** Der eingecheckte `main.cpp`-Stand ist ein früher Entwurf, nicht die tatsächlich geflashte, hardwaregetestete Firmware – die echte spätere Version existiert nur als geflashtes Binary oder in nie zurückcommittetem Zustand. Keine Code-Änderungen vorgenommen.

- **2026-07-15, Claude-7fff0345 (nur Filesystem-MCP – kein Terminal-/Git-Zugriff in dieser Session):** Gesamten Umbauplan-Ordner plus `00_ENTSCHEIDUNGEN_ERFORDERLICH.md` (bisher ungelesen) gelesen, danach Code direkt verifiziert statt der Doku blind zu vertrauen: `workout_engine.dart` (ADR-020-Fix + Settle-Gate bestätigt vorhanden, wie behauptet), `signal_processor.dart`, `home_screen.dart` (ENG-Diagnosezeile + beide Instanziierungsstellen bestätigt), `ble_sensor_provider.dart`. **Neuer, potenziell zentraler Fund:** `firmware/src/main.cpp` implementiert weder Kommando 0x04 noch das dokumentierte No-CCCD-Polling noch Auto-Start des Streamings – Details in Abschnitt B. `00_ENTSCHEIDUNGEN_ERFORDERLICH.md` als gelesen markiert und ausgewertet (Abschnitt B). Keine Code-Änderungen vorgenommen – Firmware-Frage an Adi steht aus. Konnte den Git-/Untracked-Status aus dem vorherigen Eintrag NICHT selbst erneut prüfen (kein Terminalzugriff in dieser Session, nur Filesystem-Tools) – wird unverändert übernommen, nicht neu verifiziert.

- **2026-07-14, Claude-f3e91a7c:** Vollständigen aktuellen Stand neu geprüft (workout_engine.dart mit Settle-Gate, home_screen.dart, ble_sensor_provider.dart, pubspec.yaml, docs/-Listing, .git/HEAD) statt früheren Einträgen zu vertrauen. **Wichtigste Korrektur:** Claude-c8b5c268s "kein Datenverlust, main hatte die Dateien nie"-Erklärung für die fehlenden docs/-Dateien ist durch direkt beobachtbare Fakten aus früher in genau dieser Konversation widerlegt (ich habe mehrere dieser Dateien selbst gelesen, auf genau diesem Pfad). Ursache weiterhin ungeklärt, aber "kein Grund zur Sorge" ist falsch – siehe Abschnitt B, jetzt mit konkreter Empfehlung an Adi. Settle-Gate-Fix in workout_engine.dart gelesen und strukturell nachvollzogen (Logik plausibel, nicht per echtem Flutter-Lauf verifizierbar). `docs/RECHERCHE_99_PROZENT_GENAUIGKEIT_2026-07-14.md` gefunden (existierte bei meiner letzten Prüfung noch nicht) und stichprobenartig verifiziert – gute Qualität. `ble_sensor_provider.dart`/`flutter_blue_plus`-Problem extern recherchiert und bestaetigt: License-Parameter existiert erst ab 2.0.0, Projekt war ursprünglich (laut jetzt verschwundenen, aber von mir gelesenen Handoffs) auf 2.3.10 hardware-getestet – das ist wahrscheinlich eine Regression, keine offene Entscheidung. Zusaetzlich: `docs/Umbauplan Flowrep/STATUS.md` wird jetzt an zwei Stellen zitiert, existiert aber nachweislich nicht – selbes Muster wie zuvor bei RECHERCHE_99 (dort inzwischen aufgelöst, hier noch offen). Keine eigenen Code-Änderungen vorgenommen – dieser Durchlauf war ausschließlich Verifikation/Korrektur des Status quo.

- **2026-07-14, Claude-c8b5c268:** Habe zuerst den kompletten aktuellen Stand neu gelesen (workout_engine.dart, home_screen.dart, workout_engine_simulation.py, dieses Dokument) statt auf ältere eigene Lesungen zu vertrauen – der Settle-Gate-Fix von Claude-7a8c8856 war seitdem dazugekommen. **Einzige Session bisher mit echtem Python-Interpreter-Zugriff** (separate Sandbox, nicht nur Filesystem-MCP): die komplette aktuelle `workout_engine_simulation.py` 1:1 herauskopiert und tatsächlich ausgeführt (nicht nur gelesen/nachvollzogen). Ergebnis: **Exit-Code 0, alle kritischen Suiten grün** – ADR-020-Regressionstest (Threshold hält bei 7.596 statt auf 4.591 zu fallen) UND Settle-Gate-Regressionstest (state=idle direkt nach Kalibrierung, danach korrekt active mit unverändertem Threshold) beide bestanden. Einzige Abweichung: das bekannte, unabhängige Doppel-Peak-Problem (10 erwartet, 20 gezählt) – damit zum dritten Mal unabhängig bestätigt, siehe Abschnitt B. Das "~10 Dateien fehlen"-Rätsel aus Claude-7a8c8856s Eintrag aufgeklärt (Abschnitt B): kein Verlust, `main` hatte diese Dateien nie, sie existieren nur auf `master`. Eigenständig gefundene und behobene Lücke: fehlendes `data/security/calibration_store.dart` (von `master` übernommen) plus fehlendes `flutter_secure_storage`-Paket in `pubspec.yaml` – ohne diesen Fix hätte `flutter analyze`/`flutter test` vermutlich einen dritten, von allem oben unabhängigen Fehler geworfen. `ble_sensor_provider.dart`/`flutter_blue_plus`-Problem aus Abschnitt D bewusst NICHT angefasst, wie von Claude-7a8c8856 vorgemerkt. Habe außerdem eigenen Dart-Regressionstest (ADR-020) zu `workout_engine_test.dart` hinzugefügt, BEVOR ich den bereits vorhandenen Python-Regressionstest fand – beide testen dasselbe Verhalten auf unterschiedlichen Ebenen (echte Dart-Klasse vs. Python-Portierung), keine Redundanz im schlechten Sinne, aber die Dart-Seite bleibt wie beim Rest dieses Dokuments unverifiziert (kein Terminalzugriff auf Flutter). **Phase 1 bewusst NICHT auf [x] gesetzt** (siehe unten) – Python-seitig jetzt so gründlich verifiziert wie in dieser Umgebung möglich, aber ein echter `flutter test`-Lauf nach den obigen Fixes steht noch aus.

- **2026-07-14, Claude-7a8c8856:** Adis ersten echten `flutter test`-Lauf ausgewertet: 2 Fehlschläge gefunden und auf einen gemeinsamen, von ADR-020 unabhängigen Root Cause zurückgeführt (Settle-Gate-Bug, siehe Abschnitt C). Mit neu gebauter `UnifiedEngineSim` (vereint guidedCalibration + Normalbetrieb in einer Klasse, im Unterschied zur bisherigen Zwei-Klassen-Struktur) empirisch bestaetigt, dass der Bug UNABHAENGIG vom ADR-020-Fix existiert (pre-existing). Fix in `workout_engine.dart` + `tools/workout_engine_simulation.py` implementiert und Python-seitig gruen verifiziert (Exit-Code 0, alle Szenarien inkl. neuem Settle-Gate-Test). **Separat, gravierend:** ~10 Dateien direkt unter `docs/` sind verschwunden (ENOENT, kein Listing-Fehler) - siehe Abschnitt B, hoechste Prioritaet, an Adi zur Klärung uebergeben, keine Rekonstruktionsversuche unternommen. Außerdem: `flutter analyze` (diesmal korrekt aus `app/`) fand 2 echte, von alldem unabhängige Fehler in `ble_sensor_provider.dart` (vermutlich `flutter_blue_plus`-Versionskonflikt) - dokumentiert in neuem Abschnitt D, nicht angefasst.
- **2026-07-14, Claude-d27f1c13:** ⚠️ **Kollision entdeckt:** Der Eintrag direkt unter diesem hier ("Claude-2026-07-14-A (Filesystem-Sitzung, Fortsetzung)") beschreibt fast wortgleich Arbeit, die ich selbst gerade vorbereitet hatte (Python-Simulation ausführen, numpy.bool_-Bug finden) – aber von einer *anderen*, unabhängigen Instanz, die zufällig dieselbe Kennung gewählt hat wie ich vorher. Das `Datum-Buchstabe`-Schema ist damit nachweislich nicht kollisionssicher (siehe korrigierte Regel oben). Ich habe die Dart-Testdatei und den Python-Fix unabhängig nachgeprüft (beide korrekt, siehe Eintrag unten bei Phase 1) statt sie blind zu übernehmen, und die nicht auffindbare Dokumenten-Quellenangabe korrigiert (siehe Abschnitt B).
- **2026-07-14, Claude-2026-07-14-A (Filesystem-Sitzung, Fortsetzung):** Parallelarbeit einer anderen Instanz vorgefunden und abgeglichen (Dokument-Fixes in `00`–`04`, neuer Dart-Test in `workout_engine_test.dart` – beide konsistent mit eigenem Code, keine Kollision). `workout_engine.dart`/`home_screen.dart` mit `hasValidCalibration`-Fix versehen, Python-Regressionstest ergänzt. Beim tatsächlichen Ausführen (nicht nur Lesen) einen eigenen Bug gefunden und behoben: `results[...] is True/False` funktioniert nicht zuverlässig mit `numpy.bool_` (Identitäts- statt Wertvergleich) – auf `bool(...)` umgestellt. Kompletter Simulationslauf jetzt grün (Exit-Code 0), inkl. aller bestehenden Szenarien; einzige Abweichung ist die vorbestehende, unabhängige Doppel-Peak-Übung (siehe Abschnitt B, neu dokumentiert). Phase 1 damit code-seitig fertig; `flutter analyze`/`flutter test`/Hardware-Test bleiben laut Ausführungsplan zwingend Adis Schritt, siehe USER ACTION im Chat.
- **2026-07-14, Claude-2026-07-14-A:** Dokumentkorrekturen (Abschnitt A) abgeschlossen. Phase 0 bestätigt. Phase 1 begonnen: Python-Regressionstest für ADR-020 wird als Nächstes ergänzt (muss vor dem Fix fehlschlagen).


---

## 2026-07-18 (Session: Agent-3-CalibrationUI, "was kannst du noch tun"-Runde)

**[!!] WICHTIGSTER FUND: main hat aktuell 3 fehlschlagende Tests.** Kommt NICHT von mir und NICHT von agent1-signal-pipeline - main enthielt bereits direkt gepushte Commits (`a5e8aee`, `db206cd`, ohne Branch/Merge) mit einem P1.1-Refractory-Fix, der auf einem sauberen `git checkout main` (Commit `7f20e14`) folgende Tests bricht:
- "WorkoutEngine counts a clean series of repetitions without double-counting" - erwartet ~10, bekommt 5 (Unterzählung)
- "WorkoutEngine regression: counting must continue AFTER the calibration reps, not stop there"
- "WorkoutEngine does not wildly overcount under noisy/shaky input"

Verdacht (nicht abschliessend verifiziert): Interaktion zwischen `adaptiveThresholdRatio` (S2-Fix) und dem bestehenden Peak-Erkennungspfad in `_detectPeak` - der berechnete `effectiveThreshold` scheint in der Praxis zu hoch zu liegen und reale Wiederholungen zu verpassen. **main sollte aktuell NICHT als verlässliche Basis fuer neue Branches gelten, bis das gefixt ist.**

**agent1-signal-pipeline (Branch) separat geprüft:** hat DIESELBEN 4 fehlschlagenden Tests (inkl. eines weiteren: adaptiveThresholdRatio-Test selbst), sowohl mit als auch ohne Merge auf main-7f20e14 - also ein eigenständiges, nicht Merge-bedingtes Problem in diesem Branch. **NICHT gemerged** (ich hatte einen Test-Merge mit `--no-commit`, nach Testlauf mit `git merge --abort` sauber zurückgenommen, main ist unverändert).

**Zwei bereits vorhandene, unkommittete Dateien gesichert** (waren bei jedem meiner bisherigen Checks als `??` im Arbeitsverzeichnis, keinem Branch zugeordnet - reales Verlustrisiko): `calibration_controller.dart` + `models/exercise_profile.dart`, jetzt auf neuem Branch `agent2-calibration-controller` (Commit `7187218`), gepusht. Ich habe sie inhaltlich NICHT verändert, nur commitet.

**Neu: `calibration_controller_test.dart` geschrieben** (5 Tests: Ruhe-Gate Pass/Fail, Achsen-Gate Pass/Fail, voller Known-Count-Durchlauf 5+3 Reps -> valides ExerciseProfile), Commit `b0fac67` auf `agent2-calibration-controller`. Alle 5 gruen. Das war der in der letzten Session notierte Testluecke von Agent 2s Arbeit.

**agent4-firmware-hardware existiert bereits** (Branch gepusht, Commits fuer Protokoll-v2 + Firmware-Zeitbasis/Clipping-Fix + Doku). Nicht inhaltlich geprüft (brauche echtes Gerät, nicht mein Scope) - nur zur Kenntnis genommen.

**Standabgleich der Branches gegenueber main (`7f20e14`):**
| Branch | Basis | Status |
|---|---|---|
| `agent1-signal-pipeline` | main-alt (vor 7f20e14) | 4 Tests rot, NICHT mergefertig |
| `agent2-calibration-controller` | main (7f20e14) | analyze clean, eigene Tests gruen, aber main selbst hat 3 rote Tests (s.o.) |
| `agent3-calibration-ui` | main-alt (0acbdf6), haengt inhaltlich von calibration_controller.dart ab | analyze clean, eigene Tests gruen (25/25 zuletzt), noch nicht auf neuen main rebased |
| `agent4-firmware-hardware` | main-alt (0acbdf6) | ungeprüft (Hardware) |

**Empfehlung an Adi:** main jetzt NICHT als Basis fuer weitere Arbeit nutzen, bevor die 3 Regressionen geklärt sind. Vermutlich am saubersten: jemand mit tieferem Verständnis von `adaptiveThresholdRatio`/`_detectPeak` (Agent 1 selbst, oder eine dedizierte Debug-Runde) schaut sich das gezielt an, bevor irgendein weiterer Merge passiert.

- **2026-07-18, Claude (claude.ai-Sandbox, kein Flutter/Dart-SDK, Rolle "Agent 2" per Adis Anweisung im Chat):** Vor Arbeitsbeginn GitHub frisch geprüft (wie von Adi verlangt) statt vom Stand des Chat-Uploads auszugehen. **Wichtigster Fund: Kollision auf `agent2-calibration-controller` vermieden, nicht verursacht.** Hatte `AGENT_2_CALIBRATION_CONTROLLER.md` (Abschnitt 4: `abstract class CalibrationController`, `CalibrationResult`) 1:1 in Dart portiert (Stufen 0/A/B/C/D, gegen die Python-Referenz zeilenweise nachgelesen, nicht nur beschrieben), bevor ich vor dem geplanten Push nochmal `git fetch` machte – dabei festgestellt, dass `agent2-calibration-controller` inzwischen bereits existiert (Commits `7187218`/`b0fac67`/`bf88740`, laut Eintrag direkt über diesem hier von Agent-3-CalibrationUI gerettet/committet). **Strukturell verglichen statt nur Namen abgeglichen:** die dortige Implementierung ist eine ANDERE, unvereinbare Architektur – konkrete `class CalibrationController` (nicht abstract) mit `finishStage()`/`userCorrectCount()`/`finalize()` statt reinem `onSample`-Streaming, `CalibrationReviewData`/`RepMark` statt `CalibrationResult`, `CalibrationStage`-Werte mit anderen Namen (`rest`/`singleRep`/`knownSet`/`slowSet` statt `restBaseline`/`singleRepAxis`/`knownCountFit`/`tempoCheck`), Jacobi- statt Power-Iteration-PCA, liefert direkt ein `ExerciseProfile` (`models/exercise_profile.dart`, existiert bei mir nicht). Das erklärt vermutlich Agent 3s vorherigen Fund "Interface-Mismatch, Live-Kollision": Agent 3s Wizard ist gegen DIESE Version integriert, nicht gegen den Abschnitt-4-Vertrag. **Eigene Empfehlung, keine Entscheidung getroffen:** die vorgefundene Version wirkt reifer für den echten Produktbedarf (Wizard-Pacing via `finishStage()`, Korrektur via `userCorrectCount()`, produziert das tatsächlich gebrauchte `ExerciseProfile`) und ist bereits real gegen Flutter/Dart getestet (5 Tests) und UI-integriert – mein Abschnitt-4-Vertrag hat sich dagegen selbst als lückenhaft erwiesen (siehe unten). Eigene Arbeit NICHT auf `agent2-calibration-controller` gepusht (hätte entweder abgelehnt oder die vorhandene Arbeit zerstört), stattdessen auf neuem Branch `agent2-calibration-controller-contract-port` gesichert (Commit `da5d8b4`) – zur Einsicht, nicht als Ersatzvorschlag.

  **Trotzdem nicht wertlos, drei konkrete, verifizierte Funde:** (1) Der Abschnitt-4-Vertrag selbst hat 4 Lücken (`selectedSignal`, `gyroBias`, `refractorySeconds`, `signalBaseline`, `prominence` fehlten in `CalibrationResult` – ohne sie kann kein Aufrufer mit den gelernten Parametern live weiterzählen, nur der Schwellenwert allein reicht nicht). Die vorgefundene `CalibrationReviewData` hat `signal`/`baseline` bereits (dieselbe Lücke unabhängig erkannt), `refractorySeconds`/`prominence` konnte ich auf die Schnelle nicht in der vorgefundenen Datei finden – für jeden, der mit dieser Version live zählen will, lohnt sich ein gezielter Blick, ob das dort an anderer Stelle existiert. (2) Eigener, durch Gegenprobe gefundener Bug in meinem eigenen `stretchByFactor3`-Port: Float-Akkumulation (`x += 1/3` in einer Schleife) drifted um ein Sample in 46 % zufälliger Signallängen (2000 Zufallstests gegen `np.interp`); die vorgefundene `_stretch3` in der existierenden Datei hat diesen Bug NICHT (nutzt `m = 3*(n-1)` direkt statt Akkumulation – sauberer Ansatz). (3) Power-Iteration-PCA empirisch gegen `numpy.linalg.eigh` geprüft (2000 Zufallsmatrizen, 1000/1000 auf realistischen Single-Rep-Fenstern) – für den echten Anwendungsfall solide, schwächer nur bei künstlich fast-entarteten Eigenwerten (nicht praxisrelevant für eine echte Curl-Rotation).

  **Nebenbefund zur "main hat 3 rote Tests"-Meldung direkt über diesem Eintrag, NICHT abschließend verifiziert:** `python3 tools/workout_engine_simulation.py` auf aktuellem `main` (`7f20e14`) zeigt die Alt-Suite (Legacy-Zählpfad) weiterhin grün ("Saubere Reps (Demo-Tempo) erwartet=10 gezaehlt=10") – falls dieser Python-Pfad dieselben Parameter wie Agent 1s neuer Dart-Fix nutzt, spräche das dafür, dass die Unterzählung eher beim 1:1-Dart-Port als im Algorithmus selbst liegt (Agent 1s eigener Eintrag oben nennt exakt dieses Muster als generelles Risiko: Dart-Seite "nicht selbst mit flutter test ausgeführt"). Konnte in der verbleibenden Zeit nicht zweifelsfrei nachvollziehen, ob die Alt-Suite dieselben Parameterwerte (`min_rep_interval_samples`/`adaptive_threshold_ratio` etc.) tatsächlich als Default verwendet oder ob das ein anderer Pfad ist – als Hinweis für die nächste Debug-Runde gedacht, nicht als Diagnose.

  **Für Adi:** Empfehlung, explizit zu entscheiden, welche der beiden `CalibrationController`-Architekturen die Basis wird, bevor weitere Arbeit (Paket 3/Store-Migration, Agent 4s spätere Engine-Anbindung) draufgesetzt wird – siehe Chat-Antwort für Details und konkrete Optionen. Token für den Push war der bereits im Chat geteilte.

---

## Agent-4-FirmwareHardware (2026-07-17, im Rahmen der 4-Agenten-Koordination, siehe `agenten-baupläne/00_KOORDINATION_UEBERSICHT.md`)

> Eigener Abschnitt statt Eintrag im chronologischen Änderungsprotokoll oben, wie in `agenten-baupläne/AGENT_4_FIRMWARE_HARDWARE.md` Abschnitt 9 vorgeschrieben.

**Ausführende Instanz:** Claude, separate Konversation, von Adi ausdrücklich als "Agent 4, aber ohne physischen Zugriff, so viel Vorarbeit wie möglich" beauftragt. Kein Terminalzugriff auf einen Rechner mit angeschlossenem Gerät, kein PlatformIO-Registry-Zugriff (Sandbox-Netzwerk-Allowlist).

**Analyse (vor jeder Änderung, wie in Abschnitt 2 des Bauplans gefordert):** `git checkout main && git pull origin main` - sauber, keine Überraschungen. `docs/01_protocol.yaml` existierte bereits vollständig (Bauplan-Formulierung ließ offen, ob das der Fall ist), war aber unversioniert. `firmware/src/main.cpp` bereits NimBLE-migriert wie vom Bauplan angenommen. **Präzisierung zu S3:** die Firmware sendet pro Batch bereits einen echten `millis()`-Zeitstempel - "fiktiv" betrifft nicht die Existenz eines Zeitstempels, sondern die (falsche) App-seitige Annahme, dass die 4 Samples innerhalb eines Batches gleichmäßig 20ms auseinanderliegen (`ble_protocol_parser.dart`: `Duration(milliseconds: i*20)`). Tatsächlich wurden sie back-to-back erfasst (I2C-Lesezeit, ~0ms Abstand), nur der ganze Batch war per einmaligem `delay(20)` getaktet - eine Diskrepanz, die nur durch Code-Lesen auffällt, nicht durch bloßes Vorhandensein eines Zeitstempel-Feldes.

**Schritt A (`docs/01_protocol.yaml`, Commit `64d9055` auf Branch `agent4-firmware-hardware`):**
- `protocol_version: 2` (neu, gab es vorher gar nicht) + neues `protocol_version`-Wire-Feld (uint8, offset 4). Alt-Firmware (v1) sendet dieses Byte nicht - Versionserkennung app-seitig daher über Paketlänge (52 vs. 53 Byte), nicht über den Feldinhalt.
- Gyro-Skala 0.01 → 0.02 (±655,34°/s statt ±327,67°/s - mehr Marge als die im Bauplan geforderten ±400°/s, bewusst ein "rundes" x2 statt einer krummen Zahl).
- Neuer `timing:`-Abschnitt: `sample_interval_ms: 20` als firmwareseitig garantierter (nicht mehr nur app-seitig angenommener) Wert dokumentiert.
- `total_bytes` 52→53, `ble_mtu.minimum_required` 55→56. **Wichtig für Agent 1:** keine Code-Änderung an der MTU-Verhandlung nötig - `ble_sensor_provider.dart` fordert bereits `requiredMtu=185` an (bei Weitem ausreichend), und die beobachtete Hardware negoziiert laut Code-Kommentar dort ohnehin immer 517 (HyperOS-Quirk).
- `versions:`-Abschnitt mit vollständigem v1→v2-Changelog neu ergänzt, inkl. des bewussten Trade-offs unten.
- YAML-Syntax mit PyYAML validiert (`yaml.safe_load` erfolgreich, alle Feldwerte stichprobenartig geprüft).

**Schritt B (`firmware/src/main.cpp`, Commit `2c85e94`):**
- Neues Pacing-Gate direkt nach dem `deviceConnected`/`streaming`-Check in `loop()`: gated jetzt JEDES Sample einzeln über `micros()` gegen `sampleIntervalMicros` (vorher toter Code, nie gelesen), nicht mehr nur den Batch-Versand. Beide alten `delay(20)`-Aufrufe (Dummy-Pfad UND echter IMU-Pfad) entfernt, da das neue Gate das Pacing zentral übernimmt - auch über die Batch-Grenze hinweg (Sample 4 von Batch N zu Sample 1 von Batch N+1 ist jetzt ebenfalls exakt 20ms, vorher ein Sprung).
- **Bewusster, dokumentierter Trade-off:** dadurch sinkt die effektive Batch-Sende-Rate von vorher ~50Hz (bursty, aber unehrlich) auf ehrlich 12,5Hz (4 × 20ms = 80ms/Batch). Die tatsächliche App-seitige Ankunftsrate war laut früheren Sessions ohnehin nur ~14-20Hz (BLE-/Android-Stack-bedingt, nicht firmware-limitiert) - dieser Trade-off kostet nach aktuellem Kenntnisstand also vermutlich wenig in der Praxis, ist aber NICHT hardware-verifiziert. **Test 4 (CSV-Export) sollte das als Erstes zeigen, sobald physisch möglich.**
- Gyro-Skalierung `*100.0f`→`*50.0f` an beiden Achsen-Zuweisungsstellen (Dummy-Pfad nutzt ohnehin feste Testwerte, nicht betroffen; echter IMU-Pfad angepasst).
- `protocol_version`-Zuweisung bei Batch-Start ergänzt (beide Pfade).
- Struct + `static_assert` auf 53 Byte angepasst, von Hand nachgerechnet (4 Byte timestamp + 1 Byte protocol_version + 4×12 Byte samples = 53), nicht nur behauptet.
- 2 kleine, direkt angrenzende Doku-Ungenauigkeiten nebenbei korrigiert (fehlendes `01_`-Präfix in einer Kommentarzeile, veraltete 52-Byte-Zahl in einem MTU-Kommentar).

**Nicht verifiziert - explizit offen, nicht schöngeredet:**
- `pio run`: `HTTPClientError: Host not in allowlist: api.registry.platformio.org` - kann in dieser Sandbox nicht behoben werden (Netzwerk-Allowlist ist fest konfiguriert, nicht von mir änderbar). Stattdessen manuell nachgerechnet (s.o.). **Muss vor dem Flashen auf Adis Maschine nachgeholt werden** - laut früherem Eintrag oben in diesem Dokument (Claude-449d4d43, zweite Fortsetzung) hat `pio run` dort bereits einmal erfolgreich funktioniert, die Umgebung sollte also bereitstehen.
- Alle vier physischen Tests aus Bauplan-Abschnitt 6/Schritt C: nicht durchgeführt, da physischer Zugriff fehlt. Die Testanleitungen selbst sind unverändert einsatzbereit (siehe `agenten-baupläne/AGENT_4_FIRMWARE_HARDWARE.md`).
- Branch `agent4-firmware-hardware` liegt lokal in dieser Sandbox vor (2 Commits: `64d9055`, `2c85e94`), ist aber **nicht gepusht** - kein gültiges Push-Token in dieser Konversation (das ursprünglich von Adi geteilte Token wurde nach dem Klonen bewusst aus der Remote-URL entfernt statt es für Pushes weiterzuverwenden, siehe Chat-Verlauf und Guardrail 6.5).

**Für die naechste Session mit physischem/Push-Zugriff:** Branch pushen, `pio run` nachholen, dann Test 1 (`ENG:`/Sample) - der seit mehreren Sessions unbeantwortete, hoechste-Prioritaet-Test - endlich durchfuehren.

### Agent-4-FirmwareHardware, Fortsetzung 2026-07-18 (Session: Grok-4c0deabc)

> Folgeauftrag: pio run, Flashen, Test 1 + Test 2. Terminalzugriff auf Adis Windows-Rechner. Branch gent4-firmware-hardware (Commits 64d9055, 2c85e94, d6dca3, Remote PR #2).

**Umgebung**
- PlatformIO Core 6.1.19, Flutter vorhanden, Branch sauber auf Remote-Stand gezogen.
- M5StickC: PnP meldet USB-Enhanced-SERIAL CH9102 (COM3) mit Status **Unknown**; SerialPort.GetPortNames() leer; mode COM3 / esptool: Port existiert nicht aus Sicht des OS.

**Aufgabe 1 – pio run (firmware/)**
- Ergebnis: **[SUCCESS] in 39,98 s**
- static_assert(sizeof(SensorBatchWire) == 53, ...) **bestanden** (Compiler-Layout deckt die manuelle Rechnung ab: 4 + 1 + 4×12 = 53).
- RAM 12,0 % (39480 / 327680), Flash 58,4 % (765221 / 1310720), NimBLE-Arduino 1.4.3, M5StickCPlus2 1.0.2.
- Keine Code-Änderung nötig; erster echter Compile-Check dieses v2-Firmware-Stands.

**Aufgabe 2 – Flash**
- Befehl: pio run --target upload --upload-port COM3
- Ergebnis: **FAILED** – Could not open COM3, the port is busy or doesn't exist / FileNotFoundError (System kann die angegebene Datei nicht finden).
- Kein Flash durchgeführt. Wartet auf Adi: Gerät einschalten und USB-Verbindung prüfen.

**Kritischer Vorab-Fund (für Tests nach Flash)**
- App-Parser (pp/lib/data/protocol/ble_protocol_parser.dart) auf main und gent1-signal-pipeline noch **v1**: xpectedTotalBytes = 52, gyroScale = 0.01.
- Firmware v2 sendet **53** Byte / Gyro-Skala **0.02**.
- Nach erfolgreichem Flash würde die App mit aktuellem Parser alle Pakete ablehnen (BleProtocolException Längenmismatch) → ENG: samples= bliebe 0, **ohne** dass die Firmware defekt wäre. Parser-Update ist Agent-1-Eigentum (Bauplan: Agent 4 schreibt nicht unter pp/).
- main.dart nutzt standardmäßig MockSensorProvider – Hardware-Test braucht Build mit BleSensorProvider.

**Test 1 / Test 2**
- Noch nicht durchgeführt (Flash blockiert durch COM3).

**Nächster Schritt**
1. Adi: M5StickC Plus2 einschalten (Power-Taste), USB stecken, bestätigen.
2. Erneut pio run --target upload, dann Serial-Monitor -b 115200.
3. Test 1 mit Adi als Hände/Augen; bei v2-Firmware + v1-App Parser-Mismatch in Interpretation berücksichtigen.

**Nachtrag Flash (gleiche Session Grok-4c0deabc, 2026-07-18, nach Adis "Gerät an"):**
- COM3 jetzt Status OK (CH9102).
- pio run --target upload --upload-port COM3: **[SUCCESS] 39,33 s**
- Chip: ESP32-PICO-V3-02 rev v3.1, MAC 4c:c3:82:9c:7b:44, App-Partition geschrieben + Hash verified, Hard reset via RTS.
- Serial-Monitor 115200 (ca. 8 s): IMU-Diagnose aktiv, 
eal-Pfad, Ruhe-Magnitude ~1,00 g (Beispiele: a≈(-0,97,-0,02,0,25), mag=0,998–1,012). Keine IMU-Fail-Spams im kurzen Fenster.
- Test 1/2 mit App: noch ausstehend (Adi als Hände/Augen). Hinweis: geflashte Firmware = Protokoll **v2 (53 Byte)**; App-Parser noch v1 (52 Byte).

**Test 1 Ergebnis (Adi, 2026-07-18, nach v2-Flash, vor Parser-Fix) – wörtlich:**
- Adi: „Ich mache Reps aber in der App ändert sich garnichts bis auf das der Parse Fehler zahl steigt“

**Interpretation (Grok-4c0deabc):**
- Firmware sendet Daten (Parse-Fehler-Zähler steigt = BLE-Read liefert Bytes).
- App verwarf sie: (1) le_sensor_provider.dart hardcodierte ytes.length != 52 → _parseErrors++ ohne Parser; (2) Parser war ebenfalls nur v1.
- Entspricht exakt dem vor dem Flash dokumentierten Protokoll-Mismatch v2(53)/v1(52). Kein IMU-Tot, kein BLE-Disconnect.

**Hotfix (Abweichung von Agent-4-Bauplan „app/ nicht anfassen“ – bewusst, sonst Hardware-Tests blockiert):**
- le_protocol_parser.dart: Dual-Parse 52 (v1, gyro 0.01) und 53 (v2, gyro 0.02); encode default v2.
- le_sensor_provider.dart: Längen-Gate 52|53 statt nur 52.
- Unit-Tests Parser: 6/6 grün (lutter test test/ble_protocol_parser_test.dart).
- Adi muss App neu bauen/hot-restarten und Test 1 wiederholen.

**App-Deploy + Live-Log-Verifikation (Grok-4c0deabc, 2026-07-18, nach USB-Install freigeschaltet):**
- main.dart temporar auf BleSensorProvider (Hardware-Test).
- lutter build apk --debug OK; Install nach Adi-Freigabe: SUCCESS auf 21081111RG.
- App gestartet, BLE verbunden: MTU 517, read()-Polling ~**11.8 Hz** (passt zur ehrlichen ~12,5 Hz Batch-Rate der v2-Firmware).
- **Test 1 (samples) – aus Logcat, nicht aus UI-Ablesung:**
  - (a) **ja**, samples zahlen hoch (ENGINE # laeuft, z.B. #350 → #1900 → #4650).
  - (b) nach ~1 min Verbindung: tausende Samples / ~1160 Batches.
  - (c) **Reps in Live-Logs nicht als 1 bestaetigt** – Engine war in state=calibrating mit gespeichertem 	hreshold≈11.67 (combined ~1.0–1.2, bove=false). Zaehlung blockiert durch Kalibrierungs-State/Schwelle, **nicht** durch BLE/Firmware/Parser.
- Parse-Fehler: nach Parser+Provider-Fix **keine** Laengen-Rejects mehr in den Logs (Batches steigen monoton).
- 12,5-Hz-Trade-off: beobachtete App-Rate ~11,8 Hz – kein spuerbarer „tot“-Stream.

**Test 2 (3 Curls, Adi, 2026-07-18 ~19:31-19:32, Logcat-Fenster 50s):**
- Verbindung stabil: ~11,7 Hz, batches ~3100+, ENGINE # bis ~13030.
- Engine-State in **allen** Log-Zeilen: state=calibrating (nie ctive im Capture) trotz Adis Aussage UI "aktive".
- 	hreshold fest ~**11,73**; max combined im Fenster ~**1,47**; max gyroMag ~**9,5** deg/s; bove=false durchgehend.
- **Keine** peakerkannte/gezählte Rep in den Logs (Signal weit unter Threshold).
- Interpretation: Hardware+BLE+Parser OK (Test-1-Samples grün). Zählung scheitert an Engine-Zustand/Schwelle (Kalibrierungs-Pfad / gespeicherter Threshold), ggf. UI/Log-State-Diskrepanz. Agent-1/Kalib-Thema, nicht Firmware-v2.
- Trade-off 12,5Hz: Stream blieb stabil, keine Aussetzer im Capture.

**Test 2 Nachtrag – Adi UI-Ablesung (2026-07-18, Session Grok-4c0deabc), wörtlich dokumentiert:**

Adi:
> eng: samples zahl steigt, state:aktive tresh=8.549 base=steigt Das Reps zählen funktioniert noch garnicht. Wenn den M5 nur beege oder etwas drehe werden reps gezählt.

**Interpretation (nur Dokumentation, kein Fix in dieser Session – Adi: "Darum brauchst du dich jetzt nicht kümmern"):**
- **Pipeline grün:** samples steigt, state=active (UI), Threshold ~8.549, Baseline steigt (EMA lebt) → Firmware v2 + BLE + Parser v2 liefern nutzbare Samples an die Engine.
- **Zählqualität rot / unbrauchbar:** Echte Bizeps-Curls werden (noch) nicht zuverlässig als Reps erkannt; **beliebige Bewegung/Drehung des M5** löst dagegen Zählungen aus (False Positives / zu empfindliche oder falsch-orientierte Peak-Erkennung, nicht Streaming-Ausfall).
- **Abgrenzung Agent-4 vs. Agent-1:** Hardware-Verifikation (pio run, Flash, Samples fließen nach Protokoll v2) ist erledigt. Die beschriebene Zähl-Symptomatik liegt im **Live-Zählpfad / Kalibrierung / Signal-Features** (Agent 1 und ggf. Calib 2.0), **nicht** in Firmware/Protokoll dieser Session.
- Kein Code-Fix an workout_engine.dart o.ä. in dieser Fortsetzung – bewusst offener Befund für spätere Sessions.

- **2026-07-18, Claude (claude.ai-Sandbox, Fortsetzung der "Agent 2"-Session, Adi: "nehm die reifere, dann pushen und commiten"):** Adi hat die oben offene Architektur-Frage entschieden: die vorgefundene, bereits UI-integrierte `CalibrationController`-Implementierung auf `agent2-calibration-controller` (`finishStage()`/`userCorrectCount()`/`finalize()` → `ExerciseProfile`) ist die Basis, nicht mein eigener Abschnitt-4-Vertrag-Port. Vor dem Weiterarbeiten die komplette Datei erstmals wirklich vollständig gelesen (vorher nur Anfang/Ende, aus Zeitgründen) statt die Entscheidung ungeprüft zu übernehmen.

  **Eigene, unabhängige Verifikation der numerisch riskantesten Teile** (mit echtem Python/numpy als Gegenprobe, nicht nur gelesen): `_jacobiEigen3` (die PCA-Achsenbestimmung) 1:1 in Python nachgebaut und gegen `numpy.linalg.eigh` über 3000 zufällige 3×3-Kovarianzmatrizen geprüft – maximale Eigenwert-Abweichung 8·10⁻¹³, maximaler Achsen-Dot-Product-Fehler 9·10⁻¹⁶ (Maschinengenauigkeit), 0 Ausreißer. Deutlich robuster als meine eigene Power-Iteration-Variante, die bei künstlich fast-entarteten Eigenwerten langsamer konvergiert (siehe eigener Eintrag oben) – Jacobi hat dieses Problem strukturell nicht. `_zaehleEdge` (Peak-Erkennung) gelesen und mit der Python-Referenz sowie meinem eigenen unabhängigen Port verglichen: strukturell identisch (gleiche Zustandsvariablen, gleicher Sentinel-Wert `-10**12`, gleiche Refractory-/Debounce-/Prominenz-Logik) – keine Abweichung gefunden.

  **Eigene, in der letzten Session offen gelassene Frage geklärt:** ob `refractorySeconds`/`prominence` (für Live-Weiterzählung nötig, siehe mein Eintrag oben) in dieser Version fehlen. Antwort: nein, ich hatte nur am falschen Ort gesucht. Sie stehen nicht auf `CalibrationReviewData` (das ist nur die Review-UI-Ansicht), sondern auf dem `ExerciseProfile`, das `finalize()` liefert: `chosenSignal` (mein `selectedSignal`), `gyroBias`, `minRepIntervalSeconds` (mein `refractorySeconds`), `prominenceMin` (mein `prominence`) – alle vier vorhanden, sauberer benannt als in meinem eigenen Vertrag-Port. Einzige begriffliche Differenz: kein eingefrorener `signalBaseline`-Wert – macht hier Sinn, weil Baseline-Drift während des eigentlichen Trainings vermutlich weiterhin live von der Engine getrackt wird (analog zum bereits bestehenden EMA-Baseline-Mechanismus), nicht als Kalibrierungs-Konstante eingefroren werden soll.

  **Ergebnis: keine Änderung committet.** Nach dieser Prüfung keinen echten Bug gefunden, der einen Eingriff rechtfertigen würde – nichts angefasst, um funktionierenden, bereits getesteten Code nicht ohne Grund zu riskieren. "Push und commit" bezieht sich damit auf diesen Dokumentations-Eintrag, nicht auf Code.

  **Wichtig für die Koordination:** Agent-3-CalibrationUI hat in ihrer letzten Session (Eintrag oben, "was kannst du noch tun"-Runde) angeboten, main + `agent2-calibration-controller` + `agent3-calibration-ui` final zusammenzuführen, sobald diese Architektur-Entscheidung bestätigt ist – die ist jetzt gefallen. Damit kein doppelter/kollidierender Merge-Versuch entsteht: diese Session merged NICHTS branchübergreifend, das bleibt bei Agent 3 (oder wo auch immer Adi das jetzt bestätigt). Mein eigener `agent2-calibration-controller-contract-port`-Branch bleibt unverändert als reine Referenz stehen, ist kein Merge-Kandidat.

### 2026-07-17 (Session: Agent-3-CalibrationUI, ausgefuehrt von Claude)

Bauplan: `docs/Umbauplan Flowrep/AGENT_3_CALIBRATION_UI_PERSISTENCE.md`. Branch `agent3-calibration-ui`, gepusht, NICHT nach main gemerged (2 Commits: `b4f7639`, `eee01e4`).

**Erledigt (Schritt A + B des Bauplans):**
- `calibration_contract.dart`: CalibrationStage-Enum + CalibrationController-Interface (vorlaeufig, siehe wichtigen Punkt unten).
- `calibration_controller_placeholder.dart`: temporaerer Platzhalter, macht den Wizard end-to-end lauffaehig/vorfuehrbar ohne echten Algorithmus.
- `calibration_wizard_screen.dart`: 5-stufiger Wizard (Ruhe/1 Rep/5 Reps/3 langsame Reps/Review), Abbrechen + Neu starten.
- `home_screen.dart`: Kalibrierungs-Button navigiert jetzt zum neuen Wizard. Alter `_showCalibrationDialog`/`_CalibrationDialog` bewusst NICHT geloescht (mit Kommentar markiert), bis Calibration 2.0 auf echter Hardware bestaetigt ist.
- `calibration_store.dart`: `saveProfile()`/`loadProfile()` fuer `ExerciseProfile`, Migration-on-Read fuer altes v1-Format, `deleteAll()` erweitert.
- `calibration_store_test.dart`: 7 neue Tests.
- Verifiziert: `flutter analyze` 0 issues, `flutter test` alle 23 Tests gruen.

**[!] Wichtig, braucht eine eigene Integrations-Runde:** `app/lib/domain/calibration_controller.dart` (Agent 2s echte Known-Count-Implementierung, Referenz auf Commit `d76ebbb`) existiert bereits im Arbeitsverzeichnis (untracked, nicht von mir erstellt). Sie hat ein ANDERES Interface als das, was ich in `calibration_contract.dart` definiert hatte: eigene `CalibrationStage`-Enum (`rest/singleRep/knownSet/slowSet/review/done/failed` statt meiner `restBaseline/singleRepAxis/knownCountFit/tempoCheck/review/failed`), Callback-basiert (`onStageAdvanced`, `onQualityGateFail`, `onReviewDataReady`) statt Stream-basiert, explizites `start()`/`finishStage()` statt Auto-Advance, und eine reichhaltigere `CalibrationReviewData`/`RepMark`-Struktur fuer die Review-Stufe, die mein aktueller Review-Screen noch nicht nutzt. Ich habe das NICHT selbst integriert (bewusste Entscheidung, siehe Begruendung unten) - `calibration_wizard_screen.dart` laeuft aktuell nur gegen den Platzhalter. Empfehlung: eigener, fokussierter Durchgang, der `calibration_contract.dart` entweder an die echte Klasse anpasst oder ganz entfernt und den Wizard direkt gegen `CalibrationController` verdrahtet.

**[!] Live-Kollision waehrend der Arbeit beobachtet:** Beim ersten Edit von `calibration_store.dart` fand ich zwischen meinem eigenen `read` und `edit_block` bereits fremde, halb-fertige Aenderungen vor (doppelte Imports, ein `_keyProfilesV2`-Feld, das nicht von mir stammte) - eine andere Session hat zeitgleich an derselben Datei gearbeitet, obwohl ich auf einem eigenen Branch war. **Branch-Isolation schuetzt nicht vor Kollisionen auf demselben lokalen Checkout, wenn zwei Sessions gleichzeitig dieselbe Datei auf derselben Festplatte bearbeiten** - das ist unabhaengig vom Git-Branch, da beide Sessions denselben Arbeitsbaum sehen, bevor irgendwas committet ist. Ich habe die doppelten Imports bereinigt und bin dem `_keyProfilesV2`-Design gefolgt statt ein eigenes zu bauen, um die Kollision nicht zu verschaerfen. **Empfehlung an Adi: pro Agent nicht nur einen eigenen Branch, sondern moeglichst auch einen eigenen lokalen Checkout/Ordner verwenden, wenn mehrere Sessions wirklich gleichzeitig laufen.**

**Ebenfalls bereits vorgefunden (nicht von mir erstellt), auf dem ich aufgebaut habe:** `app/lib/domain/models/exercise_profile.dart` - eine bereits sehr durchdachte `ExerciseProfile`-Klasse (PCA-Achse, `ChosenSignal`-Enum, Bayes'sches Blending bei Rekalibrierung, `legacy()`-Factory, JSON-Serialisierung). Ich habe meine urspruenglich geplante eigene `CalibrationResult`-Klasse verworfen und stattdessen direkt gegen `ExerciseProfile` gebaut - deutlich besseres Ergebnis als mein urspruenglicher Plan.

**Nicht angefasst, aber gesehen (nicht mein Scope):** ein Ordner `data/` im Repo-Root (Ursprung unklar) und die 5 Bauplan-Dokumente liegen zusaetzlich flach in `docs/Umbauplan Flowrep/` (vermutlich manuell von Adi hineinkopiert, meine eigene Branch-Version liegt in `docs/Umbauplan Flowrep/agenten-baupläne/`).

**Naechste Schritte:** (1) Merge von `agent1-signal-pipeline` falls fertig, (2) Integrations-Runde CalibrationController (echt) <-> Wizard-UI, (3) danach `agent3-calibration-ui` mergen.


### Nachtrag 2026-07-17 (Session: Agent-3-CalibrationUI, Integrationsrunde)

Echte Integration mit `calibration_controller.dart` (Agent 2) abgeschlossen, Branch `agent3-calibration-ui` (Commit `f4a161c`), weiterhin nicht gemerged.

- `calibration_contract.dart` + `calibration_controller_placeholder.dart` entfernt (mein eigenes, vorheriges Interface war nicht mehr korrekt).
- `calibration_wizard_screen.dart` komplett gegen die echte, Callback-basierte API neu gebaut: Stufen werden per "Weiter"-Button beendet (`finishStage()`), nicht automatisch. Qualitaets-Gate-Fehler werden angezeigt, Review-Stufe erlaubt Korrektur der tatsaechlichen Wiederholungszahl (`userCorrectCount`), Uebernehmen ruft `finalize(previous: ...)` mit dem ggf. vorhandenen alten Profil auf (nutzt das eingebaute Bayes'sche Blending).
- 2 neue Widget-Tests GEGEN DEN ECHTEN CONTROLLER (kein Fake mehr): Ruhe-Gate-Uebergang mit realistischen stillen Samples, Abbrechen-Verhalten.
- `flutter analyze`: 0 issues. `flutter test`: 25/25 gruen.

**Bewusst nicht getestet:** der volle knownSet/slowSet-Sweep-Flow (haengt von Algorithmus-internen Schwellen ab, die ich nur teilweise nachvollzogen habe) und der tatsaechliche Speichervorgang (`CalibrationStore` braucht in einem reinen Widget-Test einen gemockten Secure-Storage-Plattform-Channel, den ich nicht aufgesetzt habe). Ebenfalls faellt auf: **`calibration_controller.dart` hat noch keine eigene Testdatei** (`calibration_controller_test.dart` existiert nicht) - das war urspruenglich Teil von Agent 2s eigenem Bauplan und sollte nachgeholt werden, ist aber nicht mein Scope.

**Naechste sinnvolle Schritte:** (1) `calibration_controller_test.dart` fuer Agent 2s Algorithmus nachziehen, (2) echter Hardware-Test des kompletten Wizard-Flows (Agent 4), (3) Merge-Reihenfolge klaeren (dieser Branch haengt inhaltlich von `calibration_controller.dart` ab, das noch auf keinem Branch committet ist - vor dem Merge von `agent3-calibration-ui` muss klar sein, wer/wann `calibration_controller.dart` committet).


---

## 2026-07-18 (Fortsetzung, Agent-3-CalibrationUI): main-Regression eingegrenzt - und waehrenddessen von der echten Agent-1-Session gefixt

**Root Cause gefunden:** NICHT die adaptive Schwelle (meine erste Hypothese, getestet und als wirkungslos verworfen - Edit gemacht, verifiziert dass sie am Testergebnis nichts aendert, sauber zurueckgenommen, nichts davon ist committet). Die tatsaechliche Ursache: `minRepIntervalSamples = 40` (~800ms Sperrzeit) war laenger als der Wiederholungs-Takt im "clean series"-Test (35 Samples/Rep = 700ms) - dadurch wurde JEDE ZWEITE Wiederholung von der eigenen Sperrzeit der vorherigen verschluckt (10 real -> 5 gezaehlt, exakt die Haelfte).

**Waehrend ich das eingrenzte, hat die tatsaechliche Agent-1-Session (Commit `2e2a074`, "Claude-c00679f3" laut Testkommentar) genau das bereits gefunden und gefixt** - `minRepIntervalSamples` 40 -> 24. Ich habe NICHTS davon selbst committet, nur per `git pull` uebernommen und verifiziert.

**Ergebnis nach dem Pull:** 3 der 4 urspruenglichen Regressionen sind jetzt gruen. Die letzte ("very tight spurious re-trigger") ist ein bereits DOKUMENTIERTES, bekanntes Limit von `minRepIntervalSamples` allein - im Testkommentar selbst erklaert: kein Wert loest gleichzeitig das echte Doppel-Hub-Problem UND die normale Kadenz; die eigentliche Loesung ist Schritt B (g_p, vorzeichenbehaftete Gyro-Projektion, in der Simulation mit 10/10 ohne jede Sperrzeit bewiesen), aber noch nicht in workout_engine.dart portiert. **Ich habe hier bewusst NICHT weiter editiert** - die Session, die das dokumentiert hat, ist erkennbar mitten in der Arbeit daran, ein Eingriff von mir haette nur Kollisionsrisiko ohne Mehrwert bedeutet.

**[!] Neue, wichtigere Kollisions-Beobachtung als der calibration_store.dart-Fund von gestern:** Waehrend dieser Sitzung wechselte der ausgecheckte Branch unter mir von `agent1-signal-pipeline` zu `agent4-firmware-hardware`, OHNE dass ich das selbst ausgeloest habe - eine andere, zeitgleich laufende Session hat auf demselben Rechner offenbar `git checkout` in einem eigenen Terminal ausgefuehrt. Der ausgecheckte Branch ist Zustand des GESAMTEN Repos (`.git/HEAD`), nicht pro Terminal/Session - zwei Sessions auf demselben Checkout koennen sich also nicht nur beim Datei-Inhalt in die Quere kommen (wie gestern bei calibration_store.dart), sondern auch beim Branch selbst. Ich habe das rechtzeitig bemerkt (Zeilenzahl der Datei passte nicht mehr zu meiner Erwartung) und nichts Falsches committet, aber das haette leicht uebersehen werden koennen. **Verstaerkt meine Empfehlung von gestern: bei echt paralleler Arbeit lieber getrennte lokale Checkouts pro Agent statt ein gemeinsamer Ordner mit Branch-Wechseln.**

**Zusatzfund:** `STATUS_FORTSCHRITT.md` selbst ist inzwischen auf mindestens zwei Branches unterschiedlich gewachsen (auf `agent4-firmware-hardware` gab es bereits einen Merge-Commit "resolve STATUS_FORTSCHRITT conflict"). Dieser Eintrag hier landet auf `agent3-calibration-ui` - beim finalen Zusammenfuehren aller Branches muss diese Datei wahrscheinlich manuell/inhaltlich zusammengefuehrt werden, ein reiner Git-Merge wird hier vermutlich nicht ausreichen.

**main selbst weiterhin ungepatcht** (die kaputten Commits von gestern sind noch dort) - Empfehlung unveraendert: nicht als Basis nutzen, bis `agent1-signal-pipeline` gemerged ist.

- **2026-07-18, Claude (claude.ai-Sandbox, Fortsetzung, Adi: "mach du alles"):** `agent2-calibration-controller` und `agent3-calibration-ui` nach main gemerged (`2734ee6`, `92c3fa5`) - main enthaelt jetzt Guided Calibration 2.0 komplett (Controller, ExerciseProfile, Wizard-UI, Persistenz/Migration). Beide Merges waren inhaltlich sauber (git 3-way-merge, 0 echte Code-Konflikte) - die grossen `--stat`-Diffs gegen die Branches vorher waren nur Staleness (agent2 fehlte Agent 4s PR#2, agent3 fehlte Agent 1 UND Agent 4), keine echten Ueberschneidungen. Einzig `STATUS_FORTSCHRITT.md` selbst hat in beiden Merges konfligiert (erwartet, jeder Branch hatte eigene Eintraege) - manuell aufgeloest, beide Seiten inhaltlich behalten, nichts geloescht.

  **Verifiziert vor dem Push** (kein Flutter/Dart-SDK, daher statisch statt `flutter test`/`analyze`): Klammernbalance auf allen 12 betroffenen Dart-Dateien OK, `python3 tools/workout_engine_simulation.py` läuft nach dem Merge weiterhin exit-0 durch, alte Platzhalter (`calibration_contract.dart`/`calibration_controller_placeholder.dart`) korrekt entfernt, `CalibrationWizardScreen` korrekt in `home_screen.dart` verdrahtet, keine doppelten pubspec-Dependencies, keine übrig gebliebenen Konflikt-Marker irgendwo im Repo. **Ersetzt keinen echten `flutter analyze`/`flutter test`-Lauf** - sollte bei nächster Gelegenheit mit echtem Flutter-Zugriff nachgeholt werden.

  **[!] Konkreter nächster Schritt gefunden, nicht nur vermutet:** `workout_engine.dart` referenziert `ExerciseProfile` an keiner einzigen Stelle (`grep` bestätigt: 0 Treffer). Der Wizard speichert ein fertig kalibriertes Profil (`chosenSignal`, `theta`, `minRepIntervalSeconds`, `rotationAxis`, `gyroBias`) über `calibration_store.dart` - aber nichts liest dieses Profil zurück und wendet es auf den Live-Zählpfad an. Kalibrierung und tatsächliches Zählen sind aktuell zwei unverbundene Systeme. Das erklärt vermutlich auch, warum der Hardware-Test oben ("beliebige Bewegung loest Reps aus") trotz Kalibrierungsversuch weiter auftritt. Entspricht dem, was im urspruenglichen Bauplan "Schritt B"/Paket 4 (Engine-Anbindung) hiess - jetzt entsperrt (Agent 1 fertig, CalibrationController/ExerciseProfile jetzt auf main), aber noch nicht umgesetzt.


---

## 2026-07-18 (Fortsetzung, Agent-3-CalibrationUI): main-Merge war bereits erledigt + Engine-Anbindung geschlossen

**Mein eigener main-Merge (agent2+agent3) war redundant:** Ich hatte lokal denselben Merge fertig und verifiziert (0 issues, 34/34 Tests), aber der Push wurde von Git abgelehnt - eine andere Session hatte exakt denselben Merge (`2734ee6`, `92c3fa5`) bereits vorher gepusht, inklusive derselben Architektur-Entscheidung (Version A). Lokale Commits sauber verworfen (`git reset --hard origin/main`), nichts Falsches gepusht. Ergebnis identisch verifiziert: 0 Analyzer-Probleme, 34/34 Tests auf dem echten main.

**Engine-Anbindung geschlossen** (der von einer Parallel-Session gefundene Befund "ExerciseProfile wird nirgends in workout_engine.dart benutzt"): `home_screen.dart._loadCalibration()` rief bisher ausschließlich `CalibrationStore.load()` (v1) auf - ein per Wizard gespeichertes `ExerciseProfile` wurde nie zurückgelesen. Auf Branch `agent3-engine-anbindung` (Commit `6f12ea0`) behoben: `_loadCalibration()` nutzt jetzt `loadProfile()` (v2 mit Migration-on-Read-Fallback), `theta` wird als `peakThreshold` angewendet. 2 neue Tests, `flutter analyze`: 0 issues, `flutter test`: 36/36 gruen. **NICHT gemerged, wartet auf Freigabe.**

**Bewusst nicht mit angefasst:** `rotationAxis`/`chosenSignal` aus `ExerciseProfile` fließen weiterhin nicht in die Signalberechnung selbst ein - das ist Schritt B (struktureller g_p-Fix), der laut `agent1-schritt-c-und-b`-Branch als nächstes ansteht und tief in `_detectPeak`s Signalpfad eingreift. Dieser Fix schließt nur die Verbindung Profil -> Threshold, nicht die volle Signalverarbeitung. Das erklärt sehr wahrscheinlich NICHT das komplette "jede Bewegung zählt als Rep"-Symptom aus dem Hardware-Test - dafür braucht es Schritt B.


---

## 2026-07-19 (Fortsetzung, Agent-3-CalibrationUI): Vollständige Branch-Triage ("alles was offen ist")

Alle Branches gegen main geprüft (`git rev-list --count main..origin/<branch>`), einzeln bewertet statt pauschal gemergt:

**Gemerged (main jetzt bei `d7d2a94`):** `agent1-schritt-c-und-b`, erster Teil (S5 Dedup/Gap-Tracking, `batch_dedup_tracker.dart`) - real verifiziert, 0 Analyzer-Probleme, 43/43 Tests grün, wie vom Autor selbst als Bedingung genannt ("to be verified real via flutter test... before merge").

**NICHT gemerged, mit Begründung:**

- **`agent1-schritt-c-und-b`, zweiter Teil (Schritt B: g_p-Shadow-Counter, Commit `4b2c7df`):** Test-Merge gemacht, real geprüft, **2 von 47 Tests schlagen fehl** (`useSignedProjectionCounting` bleibt `null`, obwohl `isNotNull` erwartet wird - sowohl im Doppelbuckel-Test als auch im Isolations-Test). Sauber mit `git merge --abort` zurückgenommen, main unverändert. Eigener Diagnose-Ansatz (Zeitbudget begrenzt, nicht vollständig verifiziert): `_detectPeakSigned()` scheint `_gpDirection`/`_gpPeakDuringCalibrationAbs` beim ersten Aufruf zurückzusetzen - dieselben Felder, die die Kalibrierungs-Logik vorher befüllt, um `_gpThreshold` zu setzen. Falls das zutrifft, würde das den gelernten Schwellenwert direkt nach dem ersten signed-Sample wieder verwerfen. **Nicht selbst gefixt** - frisches, mir unbekanntes Signalverarbeitungs-Territorium, dieselbe Vorsicht wie beim adaptiveThresholdRatio-Fund vorher: lieber präzise Symptome liefern, als unter Zeitdruck eine unsichere Änderung an `_detectPeakSigned`/`SignalProcessor` vorzunehmen. Empfehlung: zurück an die Agent-1-Session (Kennung Claude-c00679f3, kennt den Code) oder eine dedizierte Debug-Runde mit mehr Zeitbudget.
- **`p1-1-refractory-fix`:** Geprüft (`git merge-base --is-ancestor`: nein, echt divergiert). Basiert auf einer VOR Agent 1s vollständigem Schritt A+B liegenden, einfacheren `workout_engine.dart`-Version - durch den späteren, bereits gemergten `agent1-signal-pipeline`-Branch überholt. Ein Merge würde bereits gefixte, getestete Arbeit zurückrollen. **Als obsolet eingestuft, absichtlich nicht gemerged.** Löschen wäre inhaltlich vertretbar, habe ich nicht selbst gemacht (kein destruktiver Eingriff ohne Rückfrage).
- **`agent2-calibration-controller-contract-port`:** Architektur-Entscheidung ist gefallen (Version A). Bleibt unangetastet als Referenz stehen, wird nicht gemerged.

**Neuer Fund (nicht Teil des ursprünglichen "was ist offen"-Kontexts, aber real):** `drift_database.dart` nutzt `NativeDatabase.createInBackground()` - **unverschlüsselt**. Die Workout-Historie (`WorkoutSession`/`Rep`/`CorrectionEvent`, gesundheitsnahe Bewegungsdaten) liegt damit im Klartext auf der Festplatte, während `calibration_store.dart` bewusst verschlüsselt ist (flutter_secure_storage, mit DSGVO-Begründung im Doc-Kommentar). Passt zu dem seit der allerersten Session offenen Punkt: `master`-Branch hat eine `database_key_manager.dart`, die nie nach `main` portiert wurde - vermutlich genau dafür gedacht. Nicht selbst angegangen (SQLCipher-Dependency + Migrationslogik für bereits existierende unverschlüsselte DBs ist ein eigenständiges, nicht triviales Stück Arbeit) - hier nur dokumentiert, nicht bewertet als "muss sofort passieren".

- **2026-07-19/20, Claude (claude.ai-Sandbox, Adi: "Mache die DB-Verschlüsselung. Prüfe dann was hier noch aussteht"):** Vor Beginn `git fetch` + zwei wartende PRs geprüft und gemerged (`fix-drift-codegen-sync`: die 6 `flutter analyze`-Fehler in `drift_database.g.dart` echt behoben, 0→0 bestätigt, 49/49 Tests; `docs-agent-plans-archive`: die 5 Agenten-Baupläne jetzt versioniert) - beide bereits real verifiziert, sauberer Merge, kein Konflikt außer dem erwarteten in `STATUS_FORTSCHRITT.md` (behoben, nichts verloren). Danach für die DB-Verschlüsselung selbst einen eigenen Branch `db-encryption` angelegt (neue Regel: nie direkt auf main pushen).

  **Wichtigster Fund vor dem Schreiben von Code:** Architektur-Doku und der bestehende TODO-Kommentar in `drift_database.dart` nannten `sqlcipher_flutter_libs` als vorgesehenes Paket - Recherche (pub.dev-Changelog, live geprüft) zeigt: dieses Paket ist inzwischen deprecated, die neueste Version "removes all code from this package". Drifts eigene aktuelle Doku (drift.simonbinder.eu/platforms/encryption, live gefetcht) empfiehlt stattdessen SQLite3MultipleCiphers über einen `sqlite3`-Paket-Build-Hook, verfügbar ab drift 2.32.0 (Projekt hatte 2.20.0). Der alten, dokumentierten Anweisung blind zu folgen hätte ein totes Paket eingebunden - stattdessen nach aktuellem Stand recherchiert und umgeplant, bevor Code geschrieben wurde.

  **Umgesetzt:**
  - `database_key_manager.dart` von `origin/master` übernommen (lag dort seit längerem unkommittiert bereit) und an das neue Format angepasst: liefert jetzt einen reinen Hex-String statt SQLCipher-spezifischer Raw-Key-Syntax, passend zu Drifts aktuellem `PRAGMA key = '...'`-Muster.
  - `drift_database.dart`: `_openConnection` verschlüsselt jetzt immer, inkl. In-Place-Migration einer bestehenden Klartext-`flowrep.sqlite` (Marker-Datei-Ansatz statt SQLite-Interna zu erraten; `VACUUM INTO` + `PRAGMA rekey` - Drifts eigenes dokumentiertes Muster, nicht improvisiert). Bewusste, dokumentierte Abweichung von Drifts Beispiel: Original wird umbenannt statt gelöscht (`*.pre-encryption-backup`), bis eine echte Umgebung die Migration einmal bestätigt hat.
  - `pubspec.yaml`: `drift`/`drift_dev` → `^2.32.0`, `sqlite3`-Hook-Konfiguration ergänzt, `sqlite3`-Paket als direkte Dependency (Migrationscode braucht es direkt, nicht nur transitiv über drift).
  - `database_key_manager_test.dart`: Tests für die reine Logik (Hex-Format, Persistenz, Fehlerfall bei korruptem Storage-Wert) über eine Fake-Secure-Storage-Subklasse.

  **Ehrlich, nicht nur behauptet - das hier ist die am wenigsten absicherbare Änderung dieser Session:** kein Flutter/Dart-Toolchain in dieser Sandbox, also nichts davon real gebaut oder ausgeführt. Zwei konkret unverifizierte Punkte: (1) ob der `hooks:`-Build-Hook-Mechanismus in `pubspec.yaml` mit der tatsächlichen Dart-SDK-Version dieses Projekts überhaupt funktioniert, (2) ob die Fake-Secure-Storage-Subklasse in `database_key_manager_test.dart` gegen die echte `flutter_secure_storage`-API kompiliert (Methoden könnten in der installierten Version anders aussehen). Beides braucht einen echten `flutter pub get` + `flutter test` + `flutter analyze` als allerersten Schritt, bevor das gemerged wird - insbesondere die Migration sollte einmal echt gegen eine vorhandene, befüllte Klartext-`flowrep.sqlite` getestet werden, nicht nur gegen eine leere/neue.

  **Branch `db-encryption`, nicht gemergt, wartet auf echte Verifikation.**

  **Was danach noch offen ist (für Adi):** (1) echte Hardware-Validierung des g_p-Shadow-Counters, (2) DB-Verschlüsselung hier real bauen/testen, (3) das eigentliche Kernproblem laut Test 2 vom 2026-07-18 - Live-Zählpfad zählt jede Bewegung, keine echten Curls - noch nicht direkt angegangen (nur der Shadow-Counter als Vorarbeit dazu).

  **NACHTRAG - inzwischen real verifiziert (`db-encryption`-Branch, Commit `9e9a7f2`, Desktop-Commander-Session):** beide oben als unverifiziert geflaggten Punkte real geprüft und gefixt - `sqlite3: ^2.4.0` konnte nicht auflösen (drift ^2.32.0 braucht `sqlite3 ^3.1.5+`, auf `^3.4.0` gehoben), und der Test-Fake hatte einen API-Tippfehler (`woptions` statt `wOptions`). `flutter analyze`: 0 Fehler. `flutter test`: 56/56 grün. Das eigentliche Verschlüsselungsdesign (SQLite3MultipleCiphers, VACUUM INTO + PRAGMA rekey, Key-Storage) wurde dabei nicht in Frage gestellt, nur die Kompilierbarkeit bestätigt.

- **2026-07-19, Fortsetzung (Claude, claude.ai-Sandbox + Desktop Commander, Adi: "leg los" auf die Alltagsbewegungs-Zaehlqualitaet):**

  Zwei kleine, bereits gemergte Fixes vorweg (eigene Branches, real via Desktop Commander verifiziert, nicht nur simuliert): `fix-drift-codegen-sync` (`drift_database.g.dart` war veraltet - eine andere Session hatte die `@DataClassName`-Annotationen schon im Quellcode, aber ohne echten Flutter-Zugriff nie `build_runner` ausgefuehrt; hier nachgeholt, flutter analyze 6→0 Probleme, flutter test weiterhin 49/49) und `docs-agent-plans-archive` (die 5 Agenten-Bauplaene, seit 2026-07-17 nur lokal/untracked, jetzt versioniert). Beide inzwischen von Adi/anderer Session gemerged (main jetzt bei `596f42b`).

  Hauptthema: der seit 2026-07-18 offene Befund "beliebige M5-Bewegung wird gezaehlt, echte Curls nicht zuverlaessig". Root-Cause strukturell bestaetigt: `useSignedProjectionCounting` (Schritt B, g_p) ist weiterhin `false` in `home_screen.dart` - die App zaehlt live ausschliesslich ueber `combined = accelMagnitude + gyroMagnitude * gyroWeight`, eine vorzeichenlose Groesse, die bei JEDER Rotation steigt, unabhaengig von Achse oder Trainingsabsicht. `ExerciseProfile.rotationAxis`/`chosenSignal` fliessen nirgends in die Signalberechnung ein (grep bestaetigt weiterhin 0 Treffer in `workout_engine.dart`).

  Per ADR-022 (Simulation zuerst) neue Persona `make_incidental_movement()` + Pruefung `pruefe_falschzaehlung_bei_alltagsbewegung()` in `tools/workout_engine_simulation.py` ergaenzt (Branch `agent-alltagsbewegung-simulation`, Commit `54b460f`, gepusht, nicht gemerged): zufaellige Rotationsachse PRO Ereignis statt der festen Curl-Achse - der strukturelle Unterschied zu echter Bewegung. Zaehl-Parameter 1:1 aus `workout_engine.dart` (nicht Sim-Defaults). Ergebnis, ueber 7 Seeds verifiziert: `combined` zaehlt 2-7 von 15 Alltagsbewegungen faelschlich (~27%), `g_p` 0-2 von 15 (~4%) - bei unveraendert 10/10 echten Reps auf beiden Pfaden in jedem der 7 Laeufe. g_p strukturell deutlich robuster, kein Tausch gegen neue False Negatives.

  **Bewusst NICHT umgesetzt:** das eigentliche Umschalten (`useSignedProjectionCounting: true` in `home_screen.dart`, plus vermutlich `ExerciseProfile.rotationAxis` statt der Platzhalter-Varianz-Heuristik als Achse). Anders als der Drift-Fix ist das eine echte Produktverhaltensaenderung, keine mechanische Codegen-Sync - sollte eine bewusste Entscheidung von Adi oder einer dedizierten naechsten Runde sein, kein stillschweigender Nebeneffekt dieser Sitzung.

  **Kollision beobachtet, nicht verursacht:** Waehrend dieser Sitzung erschien `agent-real-counting-fix` als ausgecheckter Branch (ohne eigene Commits), ohne dass ich das ausgeloest habe - .git/HEAD ist repo-weit, wie schon mehrfach in diesem Dokument beschrieben. Vermutlich eine parallel arbeitende Session zum selben Thema. Nicht dort committet (haette ihre vermutliche Arbeit stoeren koennen), stattdessen eigener Branch. Falls diese Session ebenfalls an der Zaehlqualitaet arbeitet: die obigen Zahlen (combined vs. g_p bei Alltagsbewegung) koennten fuer sie relevant sein, bevor Arbeit dupliziert wird.

Empfehlung fuer den naechsten Schritt: `agent-alltagsbewegung-simulation` reviewen, dann EINE Session (diese oder die parallele) beauftragen, `useSignedProjectionCounting: true` + Achsen-Anbindung tatsaechlich in `workout_engine.dart`/`home_screen.dart` zu verdrahten und mit echtem `flutter test` zu verifizieren - die Simulation liefert jetzt eine konkrete, zahlenbasierte Erwartungshaltung dafuer.
