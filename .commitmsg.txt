fix(phase4): Engine-Kalibrierung im Integrationstest nachgeruestet

Deadlock- und Viewport-Fix von origin (41cabde) real mit flutter test
bestaetigt (~1s statt 10-Minuten-Timeout).

Neuer, eigener Fund: debugSetHasCalibration() setzt nur das UI-Flag,
nicht WorkoutEngine.hasValidCalibration - Engine ging bei Bewegung
nach calibrating statt active, simulierter Rep wurde als Kalibrierungs-
Peak verbraucht statt gezaehlt. Fix: notifier.engine.applyCalibration()
direkt danach. Per echtem Engine-Log bestaetigt: state=active korrekt.

Reps bleiben trotzdem bei 0 - drei Hypothesen (gP-Autoritaet, Ghost
Gate, Debounce/Prominenz-Parameter) einzeln per Quellcode+Log wider-
legt, tatsaechliche Ursache noch offen. Details in STATUS_FORTSCHRITT.md.

Nicht nach main gemerged.

Session: Claude-106b941b
