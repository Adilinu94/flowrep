# Flutter/Dart
app/.dart_tool/
app/.packages
app/build/
app/android/build/
app/.flutter-plugins
app/.flutter-plugins-dependencies
**/*.g.dart
app/ios/
app/web/
app/linux/
app/macos/
app/windows/

# PlatformIO
firmware/.pio/
firmware/.vscode/

# IDE
app/.idea/
*.iml

# OS
.DS_Store
# Windows-Shell-Artefakt, tauchte wiederholt im Repo-Root auf
nul
Thumbs.db
desktop.ini

# Backups & interne Verzeichnisse (nicht ins Repo)
_backup_app/
.commandcode/

# Python Bytecode
__pycache__/
*.pyc
*.pyo

# Temporaere Dateien / Dumps / Logs (Root + App)
*.log
flutter_test_verify_claude.txt
screen_calib.png
flowrep_device_dump.bin
analyze_main.log
test_main.log

# Device dumps, Secure-Storage pulls, lokale DB-Kopien (DSGVO / Secrets)
# Nur Repo-Root — NICHT app/lib/data/ (Source-Code)
/data/

# CV-05 local artifacts (downloaded models / session CSVs)
tools/models/
tools/logs/

# Optional local agent / session noise
.grok/
.memory/
